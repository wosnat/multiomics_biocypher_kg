# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

BioCypher-based knowledge graph for multi-omics data on marine cyanobacteria *Prochlorococcus* and *Alteromonas*. Integrates genomic data, proteins, and experimental omics results (RNA-seq, proteomics, metabolomics) from public databases and research publications. The graph feeds into LLM agents for omics data analysis.

## Build and Run Commands

```bash
# Install dependencies (uv required)
uv sync

# Build the knowledge graph (full run)
uv run python create_knowledge_graph.py

# Fast iteration: test mode (100 items per adapter)
uv run python create_knowledge_graph.py --test

# Include GO and/or EC hierarchy nodes/edges
uv run python create_knowledge_graph.py --go --ec

# Re-fetch all data (bypass cache)
uv run python create_knowledge_graph.py --no-cache

# Custom output directory
uv run python create_knowledge_graph.py --output-dir ./my-output/

# Run adapter unit tests (excluding slow integration tests and KG tests)
pytest -m "not slow and not kg"

# Run a single test file
pytest tests/test_omics_adapter_organism_gene.py -v

# Run the full integration test (builds entire graph, ~1 hour)
pytest -m slow

# Run KG validity tests (requires running Docker graph at localhost:7687)
pytest tests/kg_validity/ -v
pytest -m kg                          # same thing via marker
pytest tests/kg_validity/ --neo4j-url bolt://localhost:7687  # explicit URL

# Docker deployment (builds graph + Neo4j, APOC plugin auto-installed)
docker compose up -d
# Neo4j at localhost:7474 (HTTP), localhost:7687 (Bolt, no auth)
# APOC core available in post-process and deploy containers
# Biochatter UI at localhost:8501
```

## CLI Flags for create_knowledge_graph.py

`create_knowledge_graph.py` accepts the following command-line options:

| Flag | Default | Description |
|---|---|---|
| `--test` | off | Test mode: stop each adapter after 100 items |
| `--go` | off | Download and write GO nodes/edges |
| `--ec` | off | Download and write EC nodes/edges |
| `--no-cache` | off | Re-fetch all data instead of reusing cached files |
| `--output-dir PATH` | `./biocypher-log/example_knowledge_graph/` | Output directory for CSV exports |

## Architecture

### Adapter Pattern
Each data source has a dedicated adapter in `multiomics_kg/adapters/` implementing:
- `get_nodes()` — Generator yielding `(node_id, label, properties)` tuples
- `get_edges()` — Generator yielding `(edge_id, source_id, target_id, edge_type, properties)` tuples
- `download_data(cache=True)` — Fetches/parses data with caching

#### String property sanitization
BioCypher wraps string fields in single quotes in the CSV output. Any string property value containing a single quote (`'`) or a pipe (`|`) will break `neo4j-admin import`. **All string properties must be sanitized before yielding from `get_nodes()` or `get_edges()`:**
```python
def _clean_str(value: str) -> str:
    return value.replace("'", "^").replace("|", "")
```
Define this helper locally in each adapter and apply it to every string field in node/edge property dicts. The array delimiter is also `|` (see `biocypher_config.yaml`), so pipe characters in values would corrupt array fields too.

**This applies to computed/literal strings too, not just external data.** Never use `|` or `'` as a separator or literal character when constructing string property values in code. For example, use ` :: ` as a field separator in computed summary strings, not ` | `. `clean_text()` silently converts `|` → `,` and `'` → `^`, so using them as intentional separators would corrupt the value without any error.

#### Controlled vocabularies
Every value set a property or edge property can take (`evidence`, `sources`, `level_kind`, `substrate_depth`, …) is declared in `config/controlled_vocabularies.yaml` — THE source of truth — and published as `ControlledVocabulary` nodes (`multiomics_kg/utils/controlled_vocab.py` loader + `controlled_vocabulary_adapter.py`, node-only, `DataSource`-adapter pattern) so consumers stop hard-coding value sets that drift silently into wrong answers. Five house rules govern every vocabulary the KG adds: **R1** — lowercase `snake_case` for values the KG mints; an external database's own controlled term (InterPro's `interpro_type`, InterProScan's `libraries`, NCBIfam's `family_type`) is preserved verbatim instead. **R1b** — namespace a value only when the same property holds values from different ontologies across labels (`level_kind`), not when it lives on one label (`interpro_type`). **R2** — every `sources` value corresponds to a `DataSource` node whose `id` property is `data_source:<value>` (join as `d.id = 'data_source:' + s`), so provenance is joinable. **R3** — do not materialize a threshold over a count the node already publishes (`is_promiscuous`-shaped properties are deleted, not renamed — the threshold is documented and queried instead). **R4** — one score name per concept, on one scale: `evidence_score` is a float in `[0,1]` everywhere, with `signal_count`/`signals` published so `round(score × signal_count)` recovers the raw count. **R5** — no native `bool`; a two-state fact is a categorical string naming both states meaningfully (`substrate_depth: most_specific | inherited`, not `true`/`false`) — BioCypher does not round-trip adapter-emitted `bool`. `Schema_info.controlled_vocabularies_hash` (sha256 over the emitted set) detects drift between a build and its checkout. See `docs/kg-changes/vocabulary-contract.md`.

Most adapters have a single-source class (e.g., `CyanorakNcbi`) and a **Multi*** wrapper (e.g., `MultiCyanorakNcbi`) that iterates over all configured strains/papers and aggregates nodes/edges. `create_knowledge_graph.py` only instantiates the Multi* wrappers.

### Key Adapters
- **uniprot_adapter.py** — Proteins, genes, organisms from UniProt API
- **cyanorak_ncbi_adapter.py** — Genomic data from GFF/GenBank files; also emits `organism taxon` nodes
- **omics_adapter.py** — Differential expression results from `paperconfig.yaml` files; creates `Publication` nodes, `Experiment` nodes, `Has_experiment` edges, `Tests_coculture_with` edges, and `Changes_expression_of` expression edges (Experiment → Gene)
- **go_adapter.py** — Gene Ontology terms and GO→protein edges
- **ec_adapter.py** — Enzyme Commission number hierarchy
- **pathway_adapter.py** — Metabolic/signaling pathway associations
- **functional_annotation_adapter.py** — Gene→GO edges (per-strain from `gene_annotations_merged.json`), Gene→Pfam edges + Pfam/PfamClan nodes (from `pfam_ids` in merged annotations + Pfam reference data)
- **ortholog_group_adapter.py** — OrthologGroup nodes and Gene_in_ortholog_group edges from pre-computed `ortholog_groups` field in `gene_annotations_merged.json`
- **metabolism_adapter.py** — Reaction + Metabolite nodes + Gene_catalyzes_reaction / Reaction_has_metabolite / Reaction_in_kegg_pathway / Metabolite_in_pathway edges (reads `cache/data/kegg/kegg_data.json` built by prepare-data step 6)
- **tcdb_adapter.py** — TCDB transport ontology + substrate→Metabolite bridge + cross-ontology bridges to Pfam and GO. Reads `cache/data/tcdb/{tcdb_hierarchy,tcdb_pruned}.json` built by step 6, plus each strain's `<strain>.tcdb.calls.json` for per-call edge evidence. Pruned to gene-annotated TC IDs **+ their ancestors only** (no downward walk). `Gene_has_tcdb_family` carries two sources (eggNOG + diamond) — see Key graph facts
- **cazy_adapter.py** — CAZy carbohydrate-active-enzyme ontology (observed-only; CAZY_CLASSES table from `multiomics_kg/utils/cazy_utils.py`)
- **psortb_adapter.py** — PSORTb subcellular-localization ontology (flat, scored, 1:1; `SubcellularLocalization` nodes + `Gene_has_subcellular_localization` edges from `psortb_localization`/`psortb_score` in merged annotations; vocab in `multiomics_kg/utils/psortb.py`)
- **signalp_adapter.py** — SignalP signal-peptide-type ontology (flat, scored, 1:1; `SignalPeptideType` nodes + `Gene_has_signal_peptide_type` edges from `signalp_type`/`signalp_probability`/`signalp_cleavage_*` in merged annotations; vocab + parser in `multiomics_kg/utils/signalp.py`)
- **interpro_adapter.py** — InterProScan InterPro-entry ontology (hierarchical, functional; `InterproEntry` nodes (name/interpro_id/interpro_type/level/sparse `description`) + `Gene_has_interpro_entry` edges (coords/evalue/`evalue_library`/libraries — **no `score`**, see Key graph facts) + `Interpro_entry_is_a_interpro_entry` hierarchy + `Pfam_in_interpro_entry` bridge + Layer-A `Interpro_entry_related_to_ec_number`/`_cazy_family` router edges). Reads per-strain `<strain>.interproscan.calls.json` (faceted format — `libraries`/`interpro_entries`/`go_terms`, no `pathways`, no `entry_xrefs.json` sidecar) for edge evidence + `cache/data/interpro/interpro_reference.json` (prepare_data step 9, observed-only `description` mode) for node names/types/hierarchy/description; Pfam + EC node sets injected for dangling-proof bridges. See `docs/kg-changes/interpro-multi-ontology.md` (supersedes `interproscan-extension.md` / `interpro-two-layer.md`)
- **ncbifam_adapter.py** — NCBIfam/TIGRFAM ontology, split out of the InterPro layer (flat, observed-only, scored; `NcbifamFamily` nodes (name/ncbifam_id/family_type/gene_symbol/sparse `description`) + `Gene_has_ncbifam_family` edges (start/end/evalue/score — keeps both, single-library scale) + `Ncbifam_family_in_interpro_entry` bridge). Reads per-strain calls.json NCBIFAM facet + `cache/data/ncbifam/ncbifam_reference.json` (prepare_data step 9). See `docs/kg-changes/interpro-multi-ontology.md`
- **merops_adapter.py** — MEROPS peptidase ontology (hierarchical clan→family→subfamily, observed-only CAZy-pattern pruning, scored; `MeropsFamily` nodes (+ sparse `cleavage_p1_residues`/`cleavage_summary`/`known_cleavage_count`) + `Gene_has_merops_family` edges (call_class/tier/confidence_score/identity/qcov/evalue/consensus_n/best_hit_id/best_hit_kind) + `Merops_family_is_a_merops_family` hierarchy + `Merops_family_has_pfam_domain` bridge (family-level, `member_id_count`)). `MultiMeropsAnnotationAdapter` takes an injected `pfam_node_ids` arg (TCDB-bridge pattern) for dangling-proofing the bridge edge. Reads per-strain `<strain>.merops.calls.json` for edge evidence + `cache/data/merops/merops_reference.json` (prepare_data step 10) for node names/clan descriptions/typing/`pfam_bridge`/`cleavage`. See `docs/kg-changes/merops-extension.md`
- **pdf_publication_extraction.py** — LangChain-based LLM extraction of publication metadata from PDFs
- **publication_topics_adapter.py** — Publication "discusses" literature-index edges (`Publication_discusses_gene` → Gene, `Publication_discusses_kegg_pathway` → KeggTerm). Pure edge adapter; reads each paper's `publication_topics/topics_resolved.json` (produced by the extract→resolve pipeline). Edge source = the paper's Publication node id (`doi:{doi}`, DOI from paperconfig or PDF-extraction cache). Edges carry `prominence` (central|peripheral) + the extraction `evidence` quote

### Main Entry Point
`create_knowledge_graph.py` orchestrates the pipeline:
1. Initializes BioCypher instance with `config/biocypher_config.yaml`
2. Reads three registry files:
   - `data/Prochlorococcus/genomes/cyanobacteria_genomes.csv` — genome → NCBI accession/taxid mapping
   - `data/Prochlorococcus/treatment_organisms.csv` — non-genomic organisms used as edge sources (e.g., phage, coculture partners)
   - `data/Prochlorococcus/papers_and_supp/paperconfig_files.txt` — list of paths to all `paperconfig.yaml` files
3. Calls `download_data()` then `write_nodes()`/`write_edges()` for each adapter
4. Generates Neo4j import scripts via `bc.write_import_call()`

### Configuration
- `config/schema_config.yaml` — Graph schema (node types, edge types, properties, ID conventions)
- `config/biocypher_config.yaml` — BioCypher framework settings (offline mode, delimiter, array delimiter `|`)
- `config/biocypher_docker_config.yaml` — Same but with Docker-specific paths; `scripts/build.sh` copies it over the main config

### Node ID Conventions
- Genes: NCBI GeneID (integer as string)
- Proteins: UniProt accession
- Organisms: INSDC GCF assembly accession (e.g., `GCF_000011465.1`)
- Publications: DOI
- Experiments: `{doi}_{experiment_key}` (e.g., `10.1038/s41586-024-07246-9_n_limitation_med4_rnaseq`)
- GO terms: GO:XXXXXXX
- OrthologGroups: `ortholog_group:<source>:<name>` (e.g., `ortholog_group:cyanorak:CK_00000364`)
- Pfam domains: `pfam:PF00712`
- Pfam clans: `pfam.clan:CL0060`
- EC numbers: EC hierarchy string

### Docker Pipeline Stages

**Note:** `docker compose up <service>` re-cascades the FULL `build` → `import` → `post-process` chain on every
invocation, even when earlier stages already exited 0 in a prior, separate `docker compose up` call — this
compose file has no cross-invocation "already completed" memory. Cost ~5 min per redundant cascade against warm
caches. Not a correctness issue, just a planning note if scripting repeated `docker compose up <service>` calls.

1. **build** — runs `create_knowledge_graph.py`, outputs CSVs
2. **import** — runs `neo4j-admin import` from generated script. Container runs as root with `/bin/bash` entrypoint (overriding the neo4j image's entrypoint that would drop to the `neo4j` user) so `scripts/import.sh` can chmod `/output` and write `import.status` + `import.report` artifacts the KG validity suite reads. Re-running `import` on an existing graph requires stopping `deploy` and `app` first (the deploy container holds a lock on the database files in `biocypher_neo4j_volume`)
3. **post-process** — executes `scripts/post-import.sh` (the authoritative post-import script run by Docker via `cypher-shell`). Creates indexes (scalar, full-text, OrthologGroup, Pfam, Experiment), derives `expression_status` on expression edges, and computes Gene routing signal properties (`annotation_types`, `expression_edge_count`, `significant_up_count`, `significant_down_count`, `reaction_count`, `metabolite_count`, `closest_ortholog_group_size`, `closest_ortholog_genera`) plus `rank_by_effect`, `rank_up`, and `rank_down` on expression edges. `scripts/post-import.cypher` is a reference copy kept in sync for non-Docker use -- both files must have identical Cypher logic. The script groups ~30 statements into three `cypher-shell` invocations (indexes / small aggregations / `CALL IN TRANSACTIONS` writes) to avoid per-call JVM startup overhead; each group is wrapped in `time` and prints a `[timing]` line to the container log. When modifying the cypher logic, run `scripts/post-import-validate.sh > baseline.txt` against the currently deployed graph first, then rebuild and `diff baseline.txt after.txt` — the dump is deterministic and must be byte-identical for a pure refactor. `pytest tests/kg_validity/ -v` (or `-m kg`) covers the same computed properties via explicit assertions.
4. **deploy** — exposes Neo4j
5. **app** — Biochatter web UI

## Adding Omics Data from Publications

1. Create directory: `data/Prochlorococcus/papers_and_supp/<Author Year>/`
2. Create `paperconfig.yaml` there (see `/skill:paperconfig` for interactive creation):

```yaml
publication:
  papername: "Author Year"
  doi: "10.NNNN/xxxxx"  # optional — overrides PDF-extracted DOI
  papermainpdf: "data/Prochlorococcus/papers_and_supp/Author Year/paper.pdf"

  experiments:
    my_experiment:
      name: "MED4 nitrogen limitation RNA-seq"
      organism: "Prochlorococcus MED4"
      treatment_condition: "Nitrogen limitation"
      control_condition: "Replete medium"
      experimental_context: "in Pro99 medium under continuous light"
      omics_type: RNASEQ  # or PROTEOMICS, METABOLOMICS, MICROARRAY
      test_type: "DESeq2"
      treatment_type: ["nitrogen"]   # category for filtering -- list of categories
      background_factors: ["axenic", "continuous_light"]  # optional: experimental context factors
      medium: "Pro99"
      temperature: "24C"
      light_condition: "continuous light"
      light_intensity: ""
      # For coculture experiments, add:
      # treatment_organism: "Alteromonas macleodii HOT1A3"
      # treatment_taxid: 28108

  supplementary_materials:
    supp_table_1:
      type: csv
      filename: "data/.../results.csv"
      statistical_analyses:
        - id: "unique_id_within_publication"   # required; used as edge ID prefix
          experiment: my_experiment             # references experiments block
          name_col: "Gene"
          logfc_col: "log2FoldChange"
          adjusted_p_value_col: "padj"
          pvalue_threshold: 0.05
          # Optional time-series fields:
          # timepoint: "24h"
          # timepoint_hours: 24
```

3. Add the path to `data/Prochlorococcus/papers_and_supp/paperconfig_files.txt`
4. If the publication uses non-standard gene IDs (JGI catalog IDs, probesets, etc.), add `id_translation` and/or `annotation_gff` entries to the paperconfig (see "Gene ID Mapping" below), then run `bash scripts/prepare_data.sh --steps 3 4 --strains <Strain> --force`
5. Verify gene ID matching: use the `/check-gene-ids` skill; if mapping is needed, use `/fix-gene-ids`
6. The `MultiOMICSAdapter` picks it up automatically from the text file list
7. **Extract publication "discusses" edges** (literature index — run for every new paper with a PDF). Extraction is manual (LLM): `uv run python -m multiomics_kg.extraction.topics.extract --paper "Author Year"` (or `/extract-discussed-topics`). Resolution is **`prepare_data.sh` step 8** (deterministic): `bash scripts/prepare_data.sh --steps 8 --force` (or `uv run python -m multiomics_kg.download.resolve_paper_topics --papers "Author Year" --force` for one paper). Produces `publication_topics/topics_resolved.json`, which `publication_topics_adapter` reads at build time. See `plans/publication_discusses_edges.md`.
8. **Log the paper in `CHANGELOG.md`** under `## [Unreleased]` → `### Data` (the subsection sits after `### Breaking`, before `### Added`; create it if this release has none). One bullet: paper key + DOI, organism(s) + omics type, what the experiments compare, and the scale. For a *correction* to an existing paper, say what the KG asserted before and what it asserts now. Add a `### Highlights` bullet when the paper enables a question that couldn't be asked before. `/release-kg` preflight warns when a paperconfig changed since the last tag with nothing logged.

### paperconfig supplementary_materials entry types

| Type | Purpose |
|---|---|
| `csv` | Differential expression results table with `statistical_analyses` |
| `id_translation` | Pure ID mapping table (no DE data); bridges non-standard IDs to locus tags |
| `annotation_gff` | GFF3/GTF file; adds protein_id/Name bridges for a strain |
| `gene_clusters` | Cluster assignment table; creates ClusteringAnalysis + GeneCluster nodes |
| `derived_metrics_table` | Column-level scalar summaries per gene (periodicity flags, classifiers, numeric scores); creates DerivedMetric nodes + one of 3 measurement edge types per metric (quantifies/flags/classifies) |
| `metabolite_assays_table` | Per-metabolite measurements (concentrations + presence flags); creates MetaboliteAssay nodes + 2 measurement edge types per assay (quantifies/flags); mirrors `derived_metrics_table` |

**`id_translation` example** — maps JGI catalog IDs via an annotated genome CSV:
```yaml
annotation_genome_9301:
  type: id_translation
  filename: "data/.../9301_annotated_genome.csv"
  organism: "Prochlorococcus MIT9301"
  id_columns:
    - column: "ID"
      id_type: jgi_id
    - column: "uniprot_gene_name"   # "dnaA P9301_05911" → whitespace-split anchor
      id_type: gene_name
    - column: "uniprot_entry_name"  # "DNAA_PROM0" → strip _ORGANISM suffix
      id_type: uniprot_entry_name
  product_columns:
    - column: "uniprot_protein_name"
```

**`id_translation` with `generate` block** — auto-generated via diamond protein matching for cross-assembly bridging:
```yaml
id_translation_ez55_author:
  type: id_translation
  filename: "data/.../aez55_to_ez55_id_translation.csv"
  organism: "Alteromonas EZ55"
  generate:
    method: diamond_protein_match
    source_fasta: "data/.../ez55_aa.fasta"
    source_id_col: aez55_id
    img_gff: "data/.../EZ55.gff"   # optional, for header remapping
  id_columns:
    - column: "locus_tag"
      id_type: locus_tag
    - column: "aez55_id"
      id_type: old_locus_tag
```

The `generate` block causes `build_gene_id_mapping.py` (step 3) to automatically run `scripts/map_img_to_ncbi_proteins.py` when the output file is missing or `--force` is given. Fields: `method` (`diamond_protein_match`), `source_fasta` (path to source protein FASTA), `source_id_col` (output column name), `img_gff` (optional GFF for header remapping). The `--ncbi-faa` and `--gene-mapping` args are derived from the organism's genome_dir.

**`annotation_gff` example** — extracts locus_tag/old_locus_tag/protein_id bridges:
```yaml
annotation_gff_mit9301:
  type: annotation_gff
  filename: "data/.../GCF_000015965.1_ASM1596v1_genomic.gff"
  organism: "Prochlorococcus MIT9301"
```

**`gene_clusters` example** — cluster assignment table with per-analysis metadata:
```yaml
med4_kmeans_nstarvation:
  type: gene_clusters
  name: "MED4 K-means N-starvation clusters"
  filename: "data/.../med4_kmeans_clusters.csv"
  organism: "Prochlorococcus MED4"
  gene_id_col: "gene_id"
  cluster_col: "cluster"
  cluster_type: "response_pattern"
  cluster_method: "K-means (K=9)"
  omics_type: MICROARRAY
  light_condition: "continuous light"
  treatment_type: ["nitrogen_stress"]
  treatment: "N-starvation time course (0, 3, 6, 12, 24, 48h)"
  experimental_context: "MED4 cells in Pro99; sampled at 0, 3, 6, 12, 24, 48h post N-deprivation"
  experiments:                  # optional: link to Experiment nodes
    - nitrogen_stress_experiment_key
```
The entry key (`med4_kmeans_nstarvation`) becomes the analysis key in node IDs. No `clusters:` block — cluster identities come from unique values in `cluster_col`. Per-cluster descriptions (`id`, `name`, `functional_description`, `temporal_pattern`, `expression_dynamics`) are supplied by a separately extracted `cluster_extraction_{entry_key}.json` file. Optional `score_col` and `p_value_col` fields carry membership scores onto `Gene_in_gene_cluster` edges.

**`csv` with non-standard ID columns:**
```yaml
supp_table_1:
  type: csv
  organism: "Prochlorococcus MIT9301"
  id_columns:
    - column: "ID"
      id_type: jgi_id
  statistical_analyses:
    - id: "unique_id"
      ...
```

### Gene ID Mapping

`gene_id_mapping.json` (schema v2, per strain in `cache/data/<Organism>/genomes/<Strain>/`) stores a three-tier ID mapping built by `build_gene_id_mapping.py` (prepare_data step 3) from:
1. `gene_annotations_merged.json` (locus tags, old locus tags, UniProt accessions, protein IDs)
2. `id_translation` paperconfig entries (JGI IDs, probesets, etc.)
3. `annotation_gff` paperconfig entries (additional GFF bridges)
4. `csv` table `id_columns` from paperconfigs (extra identifier columns per paper)

**Three-tier ID classification:**

| Tier | ID types | Storage | Resolution |
|------|----------|---------|------------|
| 1 (gene-unique) | `locus_tag`, `locus_tag_ncbi`, `locus_tag_cyanorak`, `old_locus_tag`, `alternative_locus_tag`, `jgi_id`, `probeset_id`, `uniprot_entry_name` | `specific_lookup` (1:1) | Always used; conflicts = data errors |
| 2 (protein-level) | `protein_id_refseq`, `protein_id`, `uniprot_accession` | `multi_lookup` (1:many) | Used only when singleton for this organism (paralogs expected) |
| 3 (generic) | `gene_name`, `gene_synonym`, `gene_oln`, `em_preferred_name` | `multi_lookup` (1:many) | Used only when singleton |

**Iterative convergence**: All sources are processed in passes until no new ID-to-locus_tag mapping is added. Transitive closure is automatic (order-independent). Typically 2–3 passes.

**v2 schema** (top-level keys):
- `specific_lookup`: `{alt_id: locus_tag}` — fast 1:1 Tier 1 lookup
- `multi_lookup`: `{alt_id: [locus_tag, ...]}` — Tier 2+3, singletons usable for resolution
- `conflicts`: `{tier1_id: [lt1, lt2]}` — Tier 1 data errors for manual review
- `genes`: `{locus_tag: {tier1_ids, tier2_ids, tier3_ids}}` — per-gene ID catalogue

`gene_id_utils.py` exposes the v2 API:
- `load_mapping_v2(genome_dir)` → `MappingData`
- `resolve_row(row, name_col, id_columns, mapping_data)` → `(locus_tag | None, method_str)`
- `expand_list(raw_val)` → splits list-valued cells on `,` and `;`

**Resolution method strings**: `tier1:<col>`, `heuristic:<col>` (zero-pad / strip asterisk), `multi:<col>` (Tier 2+3 singleton), `tier1_conflict`, `ambiguous`, `unresolved`.

Diagnostic report after build: `gene_id_mapping_report.json` (per-ID-type stats, reclassification warnings).

See `docs/methods_gene_id_mapping.md` for the full scientific description.

## Custom Claude Code Skills

The `.claude/skills/` directory provides project-specific skills:
- `/paperconfig` — interactive wizard to create paperconfig.yaml
- `/extract-discussed-topics` — Stage 1 of the discuss-edges pipeline: extract the genes + KEGG pathways a paper discusses in its narrative (full PDF → LLM, 15-pp chunks, references skipped) → `publication_topics/topics.json`
- `/check-gene-ids` — validate gene ID match rates between CSV data and graph nodes
- `/fix-gene-ids` — map gene IDs to locus tags when mismatches are found
- `/cypher-queries` — run Cypher queries against Neo4j with ready-made templates
- `/add-a-strain` — end-to-end onboarding for a brand-new strain AND re-deploy of an existing strain's v2 gene-ID mapping after a paperconfig fix (register in three hardcoded sites → prepare_data → per-strain tools → snapshot → rebuild mapping → resolve papers → verify → docker rebuild → tests → loop back when tool outputs land). Diagnostic playbook in `.claude/skills/add-a-strain/references/`.
- `/add-a-tool` — scaffold a new per-strain bioinformatics tool integration (Phase 1) following the psortb-run / tcdb-diamond / eggnog-run template
- `/merops-diamond` — run diamond blastp vs. the MEROPS peptidase scan library (5,009 curated representative sequences, release 12.5) per strain over `protein.faa` → per-protein protease/peptidase-inhibitor classification into MEROPS families (clan/family/subfamily/identifier) with tcdb-diamond-parity tier policy + non-peptidase-homolog and inhibitor flags. Phase 1 produces inspectable `<strain>.merops.calls.json` artifacts (committed); **Phase 2 (KG integration) is DONE** — `MeropsFamily` ontology via `merops_adapter` (see `docs/kg-changes/merops-extension.md`). Pure sequence evidence (no eggNOG input → Wave 1 in add-a-strain). DB self-installs to `~/tools/MEROPS/` (<10 MB, `MEROPS_DATA_DIR` overridable). Phase-1 design: `docs/superpowers/specs/2026-08-17-merops-diamond-design.md`; Phase-2 design: `docs/superpowers/specs/2026-08-17-merops-kg-integration-design.md`
- `/interproscan-run` — run InterProScan 5 (Docker) per strain over `protein.faa` → per-protein domain calls across all member DBs (Pfam, NCBIfam, PROSITE, SFLD, PANTHER, Gene3D, …) integrated into InterPro entries + GO. Emits a **faceted** `<strain>.interproscan.calls.json` (per-library match rows, InterPro entry rollups with `evalue_library` not a blended score, attributed `go_terms` — no `pathways` field, no per-strain `entry_xrefs.json` sidecar) — see the Output Schema section of `.claude/skills/interproscan-run/SKILL.md`. `--normalize` re-parses cached raw output into the current faceted shape without re-running Docker. Phase 1 produces the calls.json; **Phase 2 (KG integration) is DONE** — `InterproEntry` ontology via `interpro_adapter` **and**, since 2026-08-17, a split-out `NcbifamFamily` ontology via `ncbifam_adapter` (see `docs/kg-changes/interpro-multi-ontology.md`, supersedes `docs/kg-changes/interproscan-extension.md`). Needs one-time `--prepare-image` + `--refresh-data` (image `interpro/interproscan:5.78-109.0` + ~6.4 G data under `~/tools/InterProScan/`). Phase-1 design: `docs/superpowers/specs/2026-07-22-interproscan-domains-design.md`; faceted-format redesign: `docs/superpowers/specs/2026-08-17-interpro-multi-ontology-redesign-design.md`

## Genome Data Download Pipeline

`scripts/prepare_data.sh` orchestrates all genome annotation downloads and preprocessing. Run this before `create_knowledge_graph.py` when adding new genomes or refreshing data.

```bash
# Download everything + build annotation tables (skip eggNOG — run /eggnog-run skill separately)
bash scripts/prepare_data.sh

# Force re-run all steps
bash scripts/prepare_data.sh --force

# Cyanorak server throttles sometimes — skip it when files are already cached
bash scripts/prepare_data.sh --force --skip-cyanorak

# Specific strains or steps only
bash scripts/prepare_data.sh --strains MED4 MIT9313 --force
bash scripts/prepare_data.sh --steps 1 2 --force   # rebuild annotation tables only

# Refresh metabolism caches only (bypasses prepare_data.sh; avoids re-running NCBI/UniProt):
bash scripts/prepare_data.sh --steps 6 --force

# Rerun every derived step with --force (steps 9 1 2 3 4 5 6 7 8 10; step 0 downloads excluded); mutually exclusive with --steps
bash scripts/prepare_data.sh --rebuild
```

Logs written to `logs/prepare_data_step0.log` … `logs/prepare_data_step6.log`. Monitor with `tail -f logs/prepare_data_step0.log`.

**Step 0** (`multiomics_kg/download/download_genome_data.py`) — sub-steps:
- 1: NCBI genome (GFF, protein FASTA, GBFF)
- 2: Cyanorak GFF/GBK (strains with `cyanorak_organism` only; server can be slow)
- 3: UniProt per unique taxid → `cache/data/<org_group>/uniprot/<taxid>/`
- 4: eggNOG-mapper (skipped by default; requires `EGGNOG_DATA_DIR` in `.env`)
- 5: Build `gene_mapping.csv`

**Step 1** (`multiomics_kg/download/build_protein_annotations.py`) — builds per-taxid protein annotation tables → `protein_annotations.json` per taxid. Requires step 0 (UniProt data must be cached first).

**Step 2** (`multiomics_kg/download/build_gene_annotations.py`) — merges gene_mapping.csv + eggNOG + UniProt (protein_annotations.json) → `gene_annotations_merged.json` per strain. Also lazily loads the two step-9 central reference caches (`cache/data/interpro/interpro_reference.json`, `cache/data/ncbifam/ncbifam_reference.json`) to enrich `interpro_entries`/`ncbifam_ids`/donor-gated GO-EC-CAZy/naming-recovery fields (`enrich_interpro_fields`) — **requires step 9 to have run first** (the default `STEPS` order already places 9 before 2; see Step 9). Requires step 1.

**Step 3** (`multiomics_kg/download/build_gene_id_mapping.py`) — builds extended gene ID mappings (`gene_id_mapping.json` and backward-compat `gene_mapping_supp.csv`) per strain by harvesting `id_translation` and `annotation_gff` entries from all paperconfigs. Run as module: `uv run python -m multiomics_kg.download.build_gene_id_mapping`. Required for strains with non-standard IDs (JGI catalog IDs, probesets, etc.) in publication CSVs. Requires step 2.

**Step 4** (`multiomics_kg/download/resolve_paper_ids.py`) — pre-resolves gene IDs in paper CSVs: for each `csv` supplementary table whose `name_col` is not already `locus_tag`, loads the source CSV, resolves each `name_col` value via `build_id_lookup()` (uses `gene_id_mapping.json`), and writes `<stem>_resolved.csv` alongside the original with two extra columns: `locus_tag` (NaN when unresolved) and `resolution_method`. The `omics_adapter` probes for these files and uses the pre-resolved locus tags instead of the original `name_col`, avoiding dangling edges. Run as module: `uv run python -m multiomics_kg.download.resolve_paper_ids [--force] [--papers "Paper Name"]`. Requires step 3.

**Step 5** (`multiomics_kg/download/build_og_descriptions.py`) — extracts eggNOG ortholog group descriptions from the local `eggnog.db` SQLite database and writes a lightweight JSON cache at `cache/data/eggnog/og_descriptions.json` (~1 MB). This avoids needing the 39 GB database at KG build time (e.g., in Docker). The `ortholog_group_adapter` reads from this cache first, falling back to `eggnog.db` if the cache is missing. Run as module: `uv run python -m multiomics_kg.download.build_og_descriptions [--force]`. Requires step 2.

**Step 6** (`multiomics_kg/download/build_kegg_metabolism_xrefs.py`) — Phase 1.2.2 unified pruned KEGG cache + TCDB ontology assembly. Sub-steps:
1. Downloads 4 TCDB reference TSVs → `cache/data/tcdb/raw/` (gitignored, mirrors `cache/data/kegg/raw/`): `families.tsv` (3-part family descriptions), `superfamilies.tsv` (5-part with abbreviation/superfamily metadata), `substrates.tsv` (5-part substrate→CHEBI mappings), `acc2tcid.tsv` (UniProt-acc→5-part TCID — broadest catalog, ~19.5K distinct 5-part IDs).
2. Builds the TCDB hierarchy → `cache/data/tcdb/tcdb_hierarchy.json` (committed). Seeds from the union of all four sources: every family in `families.tsv` lands as a 3-part `tc_family` node, every 5-part TCID in any of the three 5-part-bearing files becomes a `tc_specificity` node with synthesized class/subclass/family/subfamily ancestors. Substrates from `substrates.tsv` attach to specificity nodes regardless of source.
3. Walks every strain's `gene_annotations_merged.json` to identify gene-reachable {KOs, reactions, compounds, pathways} AND gene-annotated TCDB IDs (**ancestors only** — up to the tc_class root, no downward walk).
4. Computes the **subtree-substrate rollup** over the FULL TCDB hierarchy first (no MNX): for each TC ID, unions raw `CHEBI:NNNN;name` substrate strings from every tc_specificity descendant. Pure structural DFS.
5. Prunes the TCDB hierarchy to the gene-reachable subhierarchy. Gene-annotated TCIDs not present in the curated hierarchy (typically retired eggNOG IDs) are remapped to the nearest curated ancestor by truncating dot-segments and recorded in `tcdb_pruned.json`'s `seed_aliases` — the TCDB adapter consumes that map at edge-build time so no annotation is dropped.
6. Filters the rollup to kept TC IDs, then resolves only the surviving distinct substrate strings (~1.7K) via the MNX resolver into primary IDs. Order = rollup → prune → resolve, so MNX touches only what survives pruning (smallest possible scope) and ancestors capture substrates from the FULL hierarchy (not just gene-annotated subset). Result: `subtree_substrates: {tc_id: [primary_id, ...]}` written to `tcdb_pruned.json`; the adapter emits one Tcdb_family_transports_metabolite edge per pair without traversing the hierarchy.
7. Splits resolved substrate primaries into kegg.compound:* (extends the gene-reachable cpds set) and non-KEGG (chebi:*, mnx:*) buckets.
8. Computes transport-reachable pathways = ⋃ `compound→pathway` for every kegg.compound substrate; extends the gene-reachable pws set with these.
9. Prunes raw KEGG to the EXTENDED sets (catalysis ∪ transport-reachable), enriching reactions/compounds with MNX cross-refs. compound→pathway lists are filtered to extended pws (Option B).
10. Tags every compound entry with `evidence_sources` based on origin: `['metabolism']` if only catalysis-reachable, `['transport']` if only substrate-reachable, `['metabolism', 'transport']` if both. Non-KEGG primaries become `additional_compounds` entries with `['transport']`.
11. Downloads TCDB's published **Pfam→TC** and **GO→TC** maps (`tcdb_{pfam,go}_map.tsv` into `raw/`) and rolls each onto the pruned node set via `build_tcdb_bridges` (nearest surviving ancestor; targets shallower than `tc_family` dropped). Yields `pfam_bridge` / `go_bridge` = `{tc_id: {xref_id: [curated 5-part TCIDs]}}`.
12. Writes `cache/data/tcdb/tcdb_pruned.json` (`kept_tcdb_ids` + `subtree_substrates` + `seed_aliases` + `pfam_bridge` + `go_bridge`) and `cache/data/kegg/kegg_data.json` (~3-4 MB, indented JSON for git-friendly diffs).

Both `kegg_annotation_adapter` / `metabolism_adapter` (KEGG side) and `tcdb_adapter` (TCDB side) read these files — no per-adapter pruning at iteration time. The ~4 GB MNX resolver opens here only — it must be built first via `bash scripts/refresh_mnx.sh` (one-time setup; rerun only when MNX releases). The resolver location is configurable via the `MNX_DATA_DIR` env var (set in `.env`, e.g. `MNX_DATA_DIR=~/tools/mnx`) so a single ~4 GB cache can be shared between checkouts; defaults to `cache/data/mnx/` when unset. Run as module: `uv run python -m multiomics_kg.download.build_kegg_metabolism_xrefs [--force] [--refetch-raw]`. `--force` rebuilds outputs from already-cached raw inputs (~1 min, no network); `--refetch-raw` also re-downloads raw KEGG REST + TCDB TSVs (use only when KEGG/TCDB upstream releases). Step 6 also walks every paperconfig `metabolite_assays_table` entry (Phase 2 metabolomics): harvests paper-measured metabolites, extends `kegg_data.json` (with `evidence_sources` including `"metabolomics"`), and writes `cache/data/metabolomics/metabolite_id_mapping.json` (consumed by step 7). Requires step 5 + the MNX resolver.

**TCDB outage tolerance:** TCDB's CGI endpoints (`tcdb.org/cgi-bin/...`) 404 during maintenance while the site root stays up. The raw TSVs under `cache/data/tcdb/raw/` are gitignored, but `tcdb_hierarchy.json` is committed — so step 6 catches a failed TCDB download/hierarchy-rebuild, warns, and reuses the committed hierarchy; only a *missing* hierarchy is fatal. The Pfam→TC / GO→TC bridge maps fall back to the bridges already in `tcdb_pruned.json`, filtered to the current run's `kept` set (previously they degraded silently to `{}`, dropping every `Tcdb_family_has_pfam_domain` / GO bridge edge). Verified byte-identical `tcdb_pruned.json` across an outage run. Re-run with `--refetch-raw` once TCDB is back.

**Fail-loud KEGG parsers:** every `_parse_*` in `kegg_utils` calls `_require_parsed`, which raises when a non-empty KEGG response yields zero entries — that combination is always upstream format drift, never a real result. Added after KEGG switched `/link/pathway/reaction` from `path:rn00010` to `path:map00010`: the parser accepted only the `rn` form, so a fresh fetch silently produced 0 reaction→pathway links (and hence no `Reaction_in_kegg_pathway` edges) while the rn-based test fixture kept passing. Committed caches built before the switch were unaffected, which is why it stayed latent.

**Step 7** (`multiomics_kg/download/resolve_paper_metabolites.py`) — Phase 2 metabolomics. For each `metabolite_assays_table` entry, opens the source CSV and writes `<stem>_resolved.csv` (alongside the original) with two added columns: `metabolite_id` (KEGG/CHEBI/MNX primary, NaN when unresolved) and `resolution_method` (`kegg_direct` | `name_match` | `alias_override` | `unresolved` | …). Mirrors gene-side step 4. Reads only `metabolite_id_mapping.json` — does NOT load the MNX resolver. Run as module: `uv run python -m multiomics_kg.download.resolve_paper_metabolites [--force] [--papers "Paper Name"]`. Requires step 6.

**Step 8** (`multiomics_kg/download/resolve_paper_topics.py`) — Publication "discusses" edges resolution. For each paper with a `publication_topics/topics.json` (written by the manual `/extract-discussed-topics` LLM step — extraction is NOT in prepare_data), resolves narrative gene mentions to strain-specific locus tags (via `gene_id_mapping.json`) and KEGG-pathway mentions to `ko*` ids (via `kegg_data.json`), writing `topics_resolved.json` + `resolution_report.txt`. Deterministic; mirrors gene-side step 4 / metabolite step 7. Papers without a `topics.json` are skipped. Run as module: `uv run python -m multiomics_kg.download.resolve_paper_topics [--force] [--papers "Paper Name"]`. Requires step 3 (`gene_id_mapping.json`) + step 6 (`kegg_data.json`).

**Step 9** (`multiomics_kg/download/build_interpro_reference.py` + `build_ncbifam_reference.py`, 2026-08-17) — Builds the two committed **central reference caches** consumed by step 2's gene-annotation merge (`enrich_interpro_fields`). Both sub-builders log into a shared `logs/prepare_data_step9.log` (append mode). **Default `STEPS` order is `"0 9 1 2 3 4 5 6 7 8 10"` — step 9 now runs BEFORE steps 1/2** (a dependency-order change, not a renumber; step numbers/flags/logs are unchanged, only where step 9 sits in the default full-run sequence, because step 2 needs both reference caches to exist before it can enrich). Running `--steps 9` alone still works exactly as before.
  - InterPro sub-builder: downloads four InterPro `current_release` files — `entry.list` + `ParentChildTreeFile.txt` (names/types/hierarchy), `interpro2go` (~3 MB) and `interpro.xml.gz` (~42 MB, streamed gzip-wise, never fully decompressed) — and writes `cache/data/interpro/interpro_reference.json` (`{IPR: {name, type, parent, level[, go_terms][, pathways][, ec_numbers][, cazy_ids][, description]}}`, ~54K entries). `description` (400-char-capped abstract) ships in **OBSERVED-ONLY mode**: full descriptions on all 54,068 abstract-bearing entries are 27.1 MB, over the ~25 MB size gate, so the fallback keeps descriptions only for the ~13K accessions observed on some strain's `calls.json` plus is-a ancestors — see the InterproEntry Key-graph-facts bullet and Data Locations for the onboarding implication.
  - NCBIfam sub-builder (`build_ncbifam_reference.py`, new 2026-08-17): writes `cache/data/ncbifam/ncbifam_reference.json` (38,394 entries: name/ncbifam_id/family_type/gene_symbol/description), consumed by `ncbifam_adapter` for `NcbifamFamily` node properties.
  - Run as module: `uv run python -m multiomics_kg.download.build_interpro_reference [--force] [--refetch-raw] [--pathway-dbs METACYC]` / `uv run python -m multiomics_kg.download.build_ncbifam_reference [--force] [--refetch-raw]` (`--refetch-raw` re-pulls the FTP/upstream files, only on a release bump). Pure parse in `multiomics_kg/utils/interpro_reference.py`.

**Step 10** (`multiomics_kg/download/build_merops_reference.py`, moved out of step 9 on 2026-08-18) — writes the committed `cache/data/merops/merops_reference.json`: `families`/`subfamily_names`/`clans` (594 families / 139 subfamily names / 123 clans; family names from `family.txt` with scan-library type-example fallback — 100% of observed families named), plus, as of 2026-08-18, `pfam_bridge` (`{family: {pfam: member_id_count}}`, parsed from `interpro.txt`; 321 families / 440 (family, Pfam) pairs across the full MEROPS catalog — pruned at edge-build time to our 97 observed families + graph `Pfam` node ids, ~183 pairs expected in the live graph) and `cleavage` (`{family: {cleavage_p1_residues, cleavage_summary, known_cleavage_count}}`, parsed from `Substrate_search.txt`; 256 families across the full catalog). Consumed only at KG-build time by `merops_adapter` — moved out of step 9 because step 9's identity is "central references the step-2 merge needs," and MEROPS has no merge-time enrichment. `family.txt`/`clan.txt`/`merops_scan.lib` read `$MEROPS_DATA_DIR/DB/` when present (Phase-1 `/merops-diamond` install); `interpro.txt` (182 MB) and `Substrate_search.txt` (29 MB) aren't part of that install and always download straight into the gitignored `cache/data/merops/raw/`. Own log: `logs/prepare_data_step10.log`. Run as module: `uv run python -m multiomics_kg.download.build_merops_reference [--force] [--refetch-raw]`.

- `go_terms` / `pathways` are **sparse** — the key is omitted entirely (not an empty list) for entries with none, so consumers must use `meta.get("go_terms", [])`. 14,803 of 54,190 entries carry GO; 5,091 carry pathways.
- **This is entry-level and complements — does not duplicate — the per-strain calls.json `go_terms` facet** (the old per-strain `entry_xrefs.json` sidecar this bullet originally referred to was removed by the 2026-08-17 multi-ontology redesign — see `docs/kg-changes/interpro-multi-ontology.md`). The scan artifacts carry GO only for entries some protein actually matched (no `pathways` facet at all any more). The reference carries GO/pathways for **every** InterPro entry, including is-a ancestors that no gene hits directly, and is the **only** source of entry-level **EC** (5,100 of 12,994 observed entries) and **CAZy** (113) — InterProScan itself emits GO xrefs but neither pathways-as-KEGG nor EC/CAZy.
- **InterPro has no KEGG pathway xrefs.** Verified against the full release (2026-08-06): `db="KEGG"` occurs **0** times; the pathway xrefs that exist are MetaCyc (79,683) and Reactome (506,724). `KEGG` survives only as a legacy token in `interpro.dtd`'s allowed-`db` list. The KG's KEGG pathway layer stays eggNOG-KO-derived. Reactome is **excluded by default** (`DEFAULT_PATHWAY_DBS = ("METACYC",)`) — its xrefs are species-expanded and noise for marine bacteria.
- Equivalence check: over 100 MED4 proteins scanned with `--goterms --pathways`, the reference lookup reproduced **100/100** identical GO sets and **100/100** identical MetaCyc sets — confirming the xrefs are genuinely entry-level, not per-match.
- Design for folding these into the merge (fields, propagation filters, source attribution): `docs/superpowers/specs/2026-08-07-interpro-merge-source-design.md`.

## Data Locations

- **Genomic data (raw):** `data/Prochlorococcus/genomes/<Strain>/` and equivalents for Synechococcus/Alteromonas
- **Genomic data (cached):** `cache/data/<Organism>/genomes/<Strain>/`
- **Publication data:** `data/Prochlorococcus/papers_and_supp/<Author Year>/`
- **Gene mapping CSVs:** `cache/data/<Organism>/genomes/<Strain>/gene_mapping.csv`
- **Gene annotation tables:** `cache/data/<Organism>/genomes/<Strain>/gene_annotations_merged.json`
- **Extended gene ID mapping:** `cache/data/<Organism>/genomes/<Strain>/gene_id_mapping.json` (built by step 3; includes paper-derived IDs)
- **Backward-compat ID mapping:** `cache/data/<Organism>/genomes/<Strain>/gene_mapping_supp.csv` (generated alongside gene_id_mapping.json)
- **UniProt cache (taxid-keyed):** `cache/data/<org_group>/uniprot/<taxid>/uniprot_preprocess_data.json`
- **Pfam reference cache:** `cache/data/pfam/pfam_reference.json`
- **eggNOG OG descriptions cache:** `cache/data/eggnog/og_descriptions.json` (built by step 5; ~1 MB)
- **KEGG unified pruned cache:** `cache/data/kegg/kegg_data.json` (built by step 6; ~3-4 MB indented; single source for `kegg_annotation_adapter` + `metabolism_adapter`)
- **TCDB committed outputs:** `cache/data/tcdb/{tcdb_hierarchy,tcdb_pruned,tcdb_names}.json` (`tcdb_names.json` is built by the standalone T6 scraper `multiomics_kg/download/scrape_tcdb_names.py` — NOT by step 6, which only consumes it; the other two built by step 6; `tcdb_pruned.json` carries `kept_tcdb_ids` + `subtree_substrates` + `seed_aliases` + `pfam_bridge` + `go_bridge`. Raw TSVs under `cache/data/tcdb/raw/` are gitignored: 4 hierarchy TSVs + `tcdb_pfam_map.tsv` + `tcdb_go_map.tsv` + the T6 scraper's page cache `raw/pages/*.html`)
- **InterPro reference cache:** `cache/data/interpro/interpro_reference.json` (built by step 9; `{IPR: {name, type, parent, level[, go_terms][, pathways][, ec_numbers][, cazy_ids][, description]}}`, ~54K entries; all five xref/description keys are sparse — use `.get()`. `ec_numbers` on 10,849 entries, `cazy_ids` on 227 (parsed from `interpro.xml.gz` `db="EC"`/`db="CAZY"`, stored raw — consumer normalizes). `description` (2026-08-17, 400-char-capped abstract) ships **OBSERVED-ONLY**: kept only for the ~13K InterPro accessions observed on some strain's `calls.json` + is-a ancestors (12,955 non-empty of ~13K kept), because the full 54,068-entry set is 27.1 MB — over the ~25 MB size gate. This makes the reference **corpus-coupled** for `description` specifically: a brand-new strain's genuinely novel InterPro accessions won't have a description until `prepare_data.sh --steps 9 --force` is re-run after that strain's InterProScan batch lands (every other field degrades the same way it always did — snapshot, not live — this is the first field gated on scanned-strain membership rather than just release freshness). Lazily loaded inside gene-annotation build (step 2), Pfam-precedent. Raw files under `cache/data/interpro/raw/` are gitignored)
- **NCBIfam reference cache:** `cache/data/ncbifam/ncbifam_reference.json` (built by step 9, `build_ncbifam_reference.py`, 2026-08-17; 38,394 entries: name/ncbifam_id/family_type/gene_symbol/description — full corpus, no observed-only fallback needed, descriptions are short enough to fit under the size gate). Consumed by `ncbifam_adapter` for `NcbifamFamily` node properties + lazily loaded inside gene-annotation build (step 2) for naming recovery. Raw files under `cache/data/ncbifam/raw/` are gitignored.
- **MEROPS reference cache:** `cache/data/merops/merops_reference.json` (built by step 10 — moved from step 9 on 2026-08-18 — `build_merops_reference.py`; `{"families": {code: {name, clan, family_type}}, "subfamily_names", "clans": {code: {description, family_type}}, "pfam_bridge": {family: {pfam: member_id_count}}, "cleavage": {family: {cleavage_p1_residues, cleavage_summary, known_cleavage_count}}}` — family names from family.txt where curated, else the scan library's `.001`/lowest-holotype type-example name with the organism suffix stripped; dead `.9xx` homologs never name a family. `pfam_bridge` (321 families / 440 pairs) and `cleavage` (256 families) are new as of 2026-08-18, parsed from `interpro.txt` and `Substrate_search.txt` respectively, over the full MEROPS catalog — not pre-pruned to our observed families). Consumed by `merops_adapter` for `MeropsFamily` node/edge properties. Raw files under `cache/data/merops/raw/` are gitignored (`family.txt`/`clan.txt`/`merops_scan.lib` are usually unneeded there — the builder prefers `$MEROPS_DATA_DIR/DB/` from the Phase-1 install; `interpro.txt`/`Substrate_search.txt` always land in `raw/` since the Phase-1 install doesn't ship them).
- **Metabolite ID mapping (Phase 2):** `cache/data/metabolomics/metabolite_id_mapping.json` (built by step 6; alias→primary lookups across all paperconfig `metabolite_assays_table` entries; consumed by step 7)
- **MNX resolver:** location set by `MNX_DATA_DIR` env var (e.g. `~/tools/mnx`), default `cache/data/mnx/`. Holds 4 MNX TSVs + the ~4 GB SQLite resolver. Built by `bash scripts/refresh_mnx.sh`; shareable across checkouts via the env var.
- **PDF extraction cache:** `pdf_extraction_cache.json`

## KG Validity Tests

`tests/kg_validity/` contains a fast (~14s) pytest suite that connects to the live deployed Neo4j instance and validates the output graph. Tests auto-skip if Neo4j is unreachable. All tests are marked `@pytest.mark.kg`.

### Test files

| File | What it validates |
|---|---|
| `test_structure.py` | Node type presence, minimum counts (>50K genes, ≥25 organisms, ≥25 publications), orphan detection (genes without organism, proteins without organism), Gene_encodes_protein edge validation, key property presence |
| `test_biology.py` | Ecotype/clade labels per strain, katG absence in *Prochlorococcus* (Black Queen Hypothesis), all expected strains present, locus-tag → UniProt spot checks |
| `test_expression.py` | `log2_fold_change` / `adjusted_p_value` are numeric, `adjusted_p_value` ∈ [0,1], `expression_direction` ∈ {up,down}, direction/sign consistency, required properties on all edges |
| `test_organism.py` | No garbage taxonomy nodes, species on Alteromonas strains, all organisms bacterial (except Phage), organism count = 28 (23 strains + 5 treatment), precomputed stats consistency (gene_count, publication_count, experiment_count, treatment_types, omics_types), Publication.organisms ↔ OrganismTaxon.preferred_name alignment |
| `test_post_import.py` | OrthologGroup nodes exist with correct properties, Gene_in_ortholog_group membership edges exist, cross-strain bridging verified, no old homolog/ortholog edges remain |
| `test_snapshot.py` | Regression snapshot: verifies a sample of specific nodes and edges (with properties) still exist after rebuilds. Catches silent data loss. |
| `test_import_report.py` | Docker import artifact validation: reads `./output/import.status` + `./output/import.report`, fails if `neo4j-admin import` exited non-zero or skipped any relationships. Auto-skips when artifacts are missing. |

### Snapshot regression tests

`test_snapshot.py` loads `snapshot_data.json` (committed fixture) and checks that every sampled node/edge still exists with expected properties. To regenerate after intentional changes:

```bash
uv run python tests/kg_validity/generate_snapshot.py
```

### Actual Neo4j labels (BioCypher PascalCase output)

- Nodes: `Gene`, `Protein`, `OrganismTaxon`, `Publication`, `Experiment`, `OrthologGroup`, `BiologicalProcess`, `CellularComponent`, `MolecularFunction`, `EcNumber`, `KeggTerm`, `CogFunctionalCategory`, `CyanorakRole`, `TigrRole`, `Pfam`, `PfamClan`, `TcdbFamily`, `CazyFamily`, `InterproEntry`, `NcbifamFamily`, `MeropsFamily`, `SubcellularLocalization`, `SignalPeptideType`, `BriteCategory`, `ClusteringAnalysis`, `GeneCluster`, `Reaction`, `Metabolite`, `MetaboliteAssay`, `ControlledVocabulary`
- Relationships: `Gene_belongs_to_organism`, `Protein_belongs_to_organism`, `Gene_encodes_protein`, `Gene_in_ortholog_group`, `Og_has_cyanorak_role`, `Og_in_cog_category`, `Has_experiment`, `Tests_coculture_with`, `Changes_expression_of`, `Gene_involved_in_biological_process`, `Gene_located_in_cellular_component`, `Gene_enables_molecular_function`, `Biological_process_is_a_biological_process`, `Cellular_component_is_a_cellular_component`, `Molecular_function_is_a_molecular_function`, `Ec_number_is_a_ec_number`, `Gene_catalyzes_ec_number`, `Gene_has_kegg_ko`, `Kegg_term_is_a_kegg_term`, `Gene_in_cog_category`, `Gene_has_cyanorak_role`, `Gene_has_tigr_role`, `Cyanorak_role_is_a_cyanorak_role`, `Gene_has_pfam`, `Pfam_in_pfam_clan`, `Gene_has_tcdb_family`, `Tcdb_family_is_a_tcdb_family`, `Tcdb_family_transports_metabolite`, `Tcdb_family_has_pfam_domain`, `Tcdb_family_involved_in_biological_process`, `Tcdb_family_enables_molecular_function`, `Tcdb_family_located_in_cellular_component`, `Gene_has_cazy_family`, `Cazy_family_is_a_cazy_family`, `Gene_has_interpro_entry`, `Interpro_entry_is_a_interpro_entry`, `Pfam_in_interpro_entry`, `Interpro_entry_related_to_ec_number`, `Interpro_entry_related_to_cazy_family`, `Gene_has_ncbifam_family`, `Ncbifam_family_in_interpro_entry`, `Gene_has_merops_family`, `Merops_family_is_a_merops_family`, `Merops_family_has_pfam_domain`, `Gene_has_subcellular_localization`, `Gene_has_signal_peptide_type`, `Brite_category_is_a_brite_category`, `Kegg_term_in_brite_category`, `Publication_has_clustering_analysis`, `Clustering_analysis_has_gene_cluster`, `Clusteringanalysis_belongs_to_organism`, `Experiment_has_clustering_analysis`, `Gene_in_gene_cluster`, `Gene_catalyzes_reaction`, `Reaction_has_metabolite`, `Reaction_in_kegg_pathway`, `Metabolite_in_pathway`, `Organism_has_metabolite`, `Assay_quantifies_metabolite`, `Assay_flags_metabolite`, `PublicationHasMetaboliteAssay`, `ExperimentHasMetaboliteAssay`, `MetaboliteAssayBelongsToOrganism`

### Key graph facts

- Gene↔Protein linkage: explicit `Gene_encodes_protein` edges (Gene→Protein) created by UniProt adapter via RefSeq protein_id join with gene_mapping.csv
- Experiment nodes: 167 nodes with properties: `name`, `organism_name`, `treatment_type` (str[]), `background_factors` (str[]), `treatment`, `control`, `experimental_context`, `coculture_partner`, `omics_type`, `statistical_test`, `is_time_course`, `medium`, `temperature`, `light_condition`, `light_intensity`, `table_scope`, `table_scope_detail`. Node IDs: `{doi}_{experiment_key}`. Each groups the analyses for one scientific comparison within a publication. `coculture_partner` is only present on coculture experiments (null/absent on non-coculture). `table_scope` describes what genes the source DE table contains: `all_detected_genes` | `significant_any_timepoint` | `significant_only` | `top_n` | `filtered_subset`. `table_scope_detail` provides free-text clarification for ambiguous cases. `treatment_type` canonical values: `nitrogen`, `phosphorus`, `iron`, `carbon`, `salt`, `light`, `temperature`, `plastic`, `darkness`, `diel`, `viral`, `coculture`, `growth_phase`, `compartment` (list — experiments may have multiple). `compartment` is for subcellular fraction comparisons (e.g. vesicle vs whole cell) where the actual fraction lives in the Experiment `compartment` field. `background_factors` canonical values: `axenic`, `light`, `diel`, `coculture`, `chemical`, `darkness`, `viral` (list of experimental context factors that aren't the primary treatment).
- Experiment computed properties (post-import): `gene_count` (int, **cumulative** count of `Changes_expression_of` edges — sums per-timepoint counts for time-course experiments, so for a paper measuring the same set of N genes at 6 TPs `gene_count = 6N`), `distinct_gene_count` (int, **distinct** gene count via `count(DISTINCT gene)` over the experiment's edges; this is the unique-gene scalar for detection-power / pathway-background reasoning; invariant `distinct_gene_count <= gene_count`, equality only for non-time-course or single-TP experiments), `significant_up_count` (int, edges where `expression_status = 'significant_up'`), `significant_down_count` (int, edges where `expression_status = 'significant_down'`), `time_point_count` (int), `time_point_labels` (str[], `""` = no label), `time_point_orders` (int[]), `time_point_hours` (float[], `-1.0` = unknown), `time_point_totals` (int[], per-tp gene counts), `time_point_significant_up` (int[], per-tp significant-up counts), `time_point_significant_down` (int[], per-tp significant-down counts). All time_point arrays are parallel (same length = `time_point_count`), ordered by `time_point_order`. Sentinel values used because Neo4j arrays cannot contain nulls. Non-time-course experiments get one entry with `time_point_labels = [""]`; experiments with 0 edges get `gene_count = 0`, `distinct_gene_count = 0`, and empty arrays. `not_significant_count` is not stored; derive as `gene_count - significant_up_count - significant_down_count`.
- Expression edges: ~227K `Changes_expression_of` edges (Experiment → Gene), unified single type replacing the old split (`Condition_changes_expression_of` + `Coculture_changes_expression_of`). Edge properties: `time_point` (str), `time_point_order` (int), `time_point_hours` (float), `log2_fold_change` (float), `adjusted_p_value` (float), `expression_direction` (str), `significant` (str), `expression_status` (str, post-import derived: `"significant_up"` | `"significant_down"` | `"not_significant"`), `rank_by_effect` (int, post-import: rank by |log2FC| among all genes per experiment x timepoint, 1 = strongest), `rank_up` (int|null, post-import: rank by |log2FC| among significant_up genes per experiment x timepoint; null if not significant_up), `rank_down` (int|null, post-import: rank by |log2FC| among significant_down genes per experiment x timepoint; null if not significant_down). Metadata (organism, omics type, conditions) lives on the Experiment node, not the edge.
- `Has_experiment` edges: 167 — `(Publication) → (Experiment)`, one per experiment node
- `Tests_coculture_with` edges: ~80 — `(Experiment) → (OrganismTaxon)`, present only for coculture/viral experiments, links to the treatment organism
- `rank_by_effect` property on expression edges: computed by post-import Cypher, ranks genes within each (experiment, time_point_order) group by descending |log2_fold_change| (1 = strongest effect)
- OrthologGroup nodes: ~45K nodes with `name` (raw OG ID, e.g. "CK_00000364" or "COG0592@2"), `source` ("cyanorak"|"eggnog"), `taxonomic_level` (e.g. "curated", "Prochloraceae", "Proteobacteria", "Bacteria"), `taxon_id` (int), `specificity_rank` (0=curated, 1=family, 2=intermediate, 3=domain), `consensus_product` (majority-vote product from members), `consensus_gene_name` (most frequent gene name), `description` (str, nullable — eggNOG functional narrative from local `eggnog.db`; null for Cyanorak groups), `functional_description` (str, nullable — semicolon-separated majority-vote CyanorakRole hierarchical names + CogFunctionalCategory names from member genes), `member_count` (int), `organism_count` (int), `genera` (str[], e.g. ["Prochlorococcus", "Alteromonas"]), `has_cross_genus_members` ("cross_genus"|"single_genus"). Sources: Cyanorak curated clusters (Pro/Syn), eggNOG family-level OGs (Prochloraceae, Synechococcus, Alteromonadaceae), eggNOG intermediate bridging OGs (Cyanobacteria for all cyanos, Proteobacteria for all proteobacteria), eggNOG Bacteria-level COGs. `ORGANISM_GROUP_LEVELS` in `ortholog_group_utils.py` maps 8 organism groups to their configured levels. Expression propagation is query-time via 2-hop (`gene->OG<-gene`) instead of materialized ortholog edges.
- `Gene_in_ortholog_group` edges: ~223K membership edges. A gene may have 2-4 memberships (Cyanorak + family + intermediate + bacteria-level). Intermediate bridging levels enable cross-genus comparison: Cyanobacteria (1117) bridges Prochlorococcus + Synechococcus + Thermosynechococcus; Proteobacteria (1224) bridges Alteromonas + Shewanella + Pseudomonas + Ruegeria. Meiothermus has only Bacteria-level OGs.
- `Og_has_cyanorak_role` edges: OrthologGroup → CyanorakRole, majority-vote (>50% of member genes). `Og_in_cog_category` edges: OrthologGroup → CogFunctionalCategory, same majority rule.
- Pfam domain nodes: ~5,500 `Pfam` nodes (only domains referenced by genes) with `name` (description), `short_name` (Pfam shortname); ~509 `PfamClan` nodes (superfamilies) with `name` (clan name). ~89K `Gene_has_pfam` edges, ~3.5K `Pfam_in_pfam_clan` edges. Node IDs use bioregistry CURIEs: `pfam:PF*` and `pfam.clan:CL*`. Pfam reference data downloaded from `Pfam-A.clans.tsv.gz`; eggNOG shortnames resolved to PF* accessions via reverse lookup
- TcdbFamily nodes: pruned to gene-annotated TC IDs **+ their ancestors only**; **1,515 nodes** (tc_class 7 · tc_subclass 34 · tc_family 592 · tc_subfamily 596 · tc_specificity 286); 5 levels (tc_class | tc_subclass | tc_family | tc_subfamily | tc_specificity). Properties: name, tcdb_id, level (0-4), level_kind, superfamily (sparse, leaf-only), description (sparse, tc_specificity only — scraped TCDB curated text, 400-char capped), tc_class_id (post-import: pointer to root tc_class). Computed post-import: gene_count, organism_count, member_count, metabolite_count. **Node names are scraped (T6, 2026-08-18)**: all levels named from the committed `cache/data/tcdb/tcdb_names.json`, built by `python -m multiomics_kg.download.scrape_tcdb_names` (browse.php + one result.php fetch per kept family, ~352 requests, resumable, 2.5 s cadence; see the tcdb-diamond SKILL.md "Refreshing TCDB names" section). Step 6 consumes the artifact and **warns** when a strain onboarding introduces kept families the scrape hasn't covered (top-up with `--families`). Subfamilies with no upstream name stay as the bare TC id by design — `t.name = t.tcdb_id` is the fallback marker (no separate provenance property). Specificity names = citation-stripped first sentence, 150-char capped; full text in `description`. **`is_promiscuous` is DELETED** (vocabulary-contract R3 — it only ever restated `level >= 2 AND metabolite_count >= 50`, both already stored). A consumer wanting the same 13 nodes queries the threshold directly: `MATCH (t:TcdbFamily) WHERE t.level >= 2 AND t.metabolite_count >= 50 RETURN t` — 7 families: ABC `3.A.1` (554 substrates), MFS `2.A.1` (476), DMT `2.A.7`, RND `2.A.6`, MOP `2.A.66`, APC `2.A.3`, P-type ATPase `3.A.3`; plus 6 subfamilies: `2.A.1.1/.2/.3`, `2.A.6.2`, `2.A.66.1`, `3.A.1.1`. **Substrate breadth only** — for family size filter `gene_count` directly. `gene_count` here is a SUBTREE count (`*0..` descendants) — unlike `InterproEntry.gene_count`, which is direct; do not use it for ORA without collapsing levels. See `docs/kg-changes/vocabulary-contract.md`.
  - **Was ~12.9K nodes** until the 2026-08-06 pruning cleanup. The old prune also walked *down* from each seed to every descendant, so 528 gene-annotated seeds expanded to 12,902 nodes of which **12,198 (94.5%) had `gene_count = 0`** (10,292 of those `tc_specificity`). That arm existed to keep leaf substrate data reachable, but `subtree_substrates` already rolls substrates up over the FULL hierarchy pre-pruning, so every kept ancestor carries its descendants' substrates regardless. Removing it lost **zero** metabolite reachability (1,462 distinct substrate primaries before and after; `kegg_data.json` byte-identical) and dropped `Tcdb_family_transports_metabolite` from 22,483 → 11,263 edges. Every surviving node has `gene_count > 0` — though note that is trivially true under the subtree definition, since a kept ancestor inherits its descendants' genes. Node count rose 704 → 1,515 when diamond joined eggNOG as a second seed source.
  - **Semantic shift:** `level_kind = 'tc_specificity'` now selects the specificity nodes genes actually annotate (246), not every specificity node in a reachable family. Queries using that filter to recover "leaf-only" substrate semantics still work but count real transporter systems rather than all TCDB systems in the family. **But do NOT use that filter for substrates any more** — only 466 of 11,263 substrate edges sit on specificity nodes, so it made `Metabolite.transporter_count` read 0 for 83% of transported metabolites. Use `Tcdb_family_transports_metabolite.substrate_depth = 'most_specific'` instead (see the substrate-rollup bullet below). The `level >= 2 AND metabolite_count >= 50` `is_promiscuous` threshold (now a query, not a stored property — see the TcdbFamily bullet above) was calibrated against the old inflated node set and is worth revisiting on the next rebuild.
  - **Why level-gated (changed 2026-08-07):** substrate and member counts scale mechanically with hierarchy level, because the step-6 rollup materializes every descendant's substrates onto each ancestor — median `metabolite_count` is 153 at `tc_class` vs 1 at `tc_family`. The old absolute-only rule therefore fired on 5 of 7 `tc_class` and 7 of 34 `tc_subclass` nodes, which is vacuous: "Channels and Pores transports many things" is what a class *is*, not a warning. The old `member_count >= 100` arm was also dead below `tc_subclass` (max `member_count` at `tc_family` is 55) and was dropped. A `gene_count >= 500` replacement was briefly shipped and reverted the same day — it answered a different question (family *size*, not substrate breadth) and flagged substrate-poor families such as `9.B.34` with **zero** substrates. The remaining threshold sits at ≈p99 within the levels it applies to. **See `docs/kg-changes/tcdb-two-source-upgrade.md` — the MCP/explorer upgrade contract for this whole change set.**
- CazyFamily nodes: observed-only; 3 levels (cazy_class | cazy_family | cazy_subfamily). Properties: name, cazy_id, level (0-2), level_kind. Computed post-import: gene_count, organism_count.
- MeropsFamily nodes (merops-diamond, 2026-08-17): **155 nodes**, observed-only + ancestors (CAZy pruning); 3 levels (merops_clan 41 (level 0) | merops_family 97 (1) | merops_subfamily 17 (2)). Node IDs use registered bioregistry CURIEs: `merops.clan:SC` / `merops.family:S14` / `merops.family:S08A`. **No identifier-level (S01.001) nodes** — only 6 tier-1 calls exist corpus-wide; they attach at subfamily/family with the identifier preserved as edge `best_hit_id`. Properties: name (family/subfamily: MEROPS type-example name, e.g. S14 → "peptidase Clp"; clans: code), description (sparse, clans only — clan.txt fold/mechanism text), merops_id, level, level_kind, family_type (`peptidase`|`inhibitor` — I-families are kept, protease inhibitors are biology), catalytic_type (**full words**: serine/cysteine/metallo/aspartic/threonine/glutamic/asparagine_lyase/mixed/unknown; null on inhibitors). Computed post-import: gene_count + organism_count (subtree `*0..`, CAZy pattern), **peptidase_gene_count** (subtree, `call_class='peptidase'` edges only — the number to use for "how many proteases"; the gap vs gene_count flags dead-homolog-dominated families like C26/C44), member_count (direct children). The edge `Gene_has_merops_family` (Gene → node, ~4.2K; one per calls.json candidate, multi-domain proteins fan out; attaches at the called tier-truncated depth) is a **scored** ontology edge — carries **`call_class`** (`peptidase` | `inhibitor` | `nonpeptidase_homolog` — the read-first verdict; `nonpeptidase_homolog` = best hit is a catalytically dead `.9xx` relative, fold evidence NOT protease evidence; threshold-free, derived from best_hit_kind + family type), `tier` (1-3, tcdb-diamond bands: 1=identifier ≥70% id, 2=subfamily ≥40%, 3=family floor-only; ~92% tier 3 — expected remote-homology shape), `confidence_score` (0-1), `identity`, `qcov`, `evalue`, `consensus_n`, `best_hit_id` (e.g. "S14.008"), `best_hit_kind` (`holotype`=characterized | `putative` | `nonpeptidase_homolog`). Hierarchy edge `Merops_family_is_a_merops_family` (child → parent, 108; clan-unassigned families are roots). **Functional but TIER-GATED** (tcdb precedent): post-import folds `'merops'` into `annotation_types` + `informative_annotation_types` **only for tier ≤ 2 edges**; **NO `annotation_quality` bucket** (single evidence source, tier-3-dominated; bucket count stays 9); `Gene_has_merops_family` counts toward `has_any_edge`. Gene routing signals: `merops_family_count` (int, ALL edges, ungated) + `merops_classes` (str[], sorted distinct call_class values — the at-a-glance guard so a dead-homolog-only gene never reads as "1 protease"). Single evidence source (eggNOG emits no MEROPS; InterProScan has no MEROPS member DB); merged via the `merops_diamond` source (`merops_ids`, join `protein_id`); adapter reads the calls.json directly for edge evidence (tcdb/interpro pattern).
  - **2026-08-18 follow-up (Pfam bridge + cleavage specificity)** — design `docs/superpowers/specs/2026-08-18-merops-pfam-bridge-cleavage-design.md`. New bridge edge `Merops_family_has_pfam_domain` (MeropsFamily → Pfam, family-level only i.e. level-1 nodes; ~183 edges expected on rebuild — the committed reference's `pfam_bridge` block spans the full MEROPS catalog (321 families / 440 pairs), pruned at edge-build time to observed MeropsFamily nodes + the injected `pfam_node_ids`, TCDB-bridge dangling-proof pattern). Verb **`has`** (`Tcdb_family_has_pfam_domain` shape: "MEROPS's curated member proteins of this family carry this domain" — a composition statement, sound direction only; reading backward, e.g. "has PF00117 → is a C26 peptidase," is unsafe in exactly the shared-fold families `call_class` already flags as `nonpeptidase_homolog`). Carries `member_id_count` (int, how many distinct curated MEROPS identifiers back the (family, Pfam) pair — a deliberate deviation from TCDB's `curated_tcids` list, since MEROPS backing lists run to hundreds of identifiers). Post-import sets **`pfam_support`** (`corroborated`|`uncorroborated`, R5 string pair — same name/shape as `Gene_has_tcdb_family.pfam_support`) on every `Gene_has_merops_family` edge: `corroborated` iff the gene has a `Gene_has_pfam` edge to a Pfam that the edge's family (its level-1 ancestor, for subfamily-attached edges) bridges to. Advisory only — no composite score, and does **not** open a new `annotation_quality` bucket (corroboration is not a second evidence source). Three new **sparse, family-level (level-1)** MeropsFamily properties carry MEROPS's own curated cleavage-site specificity (from `Substrate_search.txt`; ≤88/97 observed families covered — absent, never empty, when uncovered): `cleavage_p1_residues` (str[], top P1 residues with ≥10% share of curated cleavages, max 3, frequency order, closed to the 20 standard amino-acid codes), `cleavage_summary` (str, e.g. `"cleaves after Lys (36%) / Arg (34%) / Glu (11%) - 39567 known cleavages (14% physiological)"`; joins `meropsFamilyFullText`), `known_cleavage_count` (int, e.g. 39,567 for S01). Honesty caveats carried by design: support spans 4 orders of magnitude (hence the count); family-level aggregation blends members (S01 blends trypsin's Lys/Arg with chymotrypsin's aromatics — the property reads "what this family is known to cut," never "what your gene cuts"); cleavage records are not independent evidence (one degradomics paper contributes many rows). A parallel MEROPS→GO bridge (`GO_annotation.txt`) was measured and **rejected**: all-kingdom member rollup gives median 19 / max 389 GO terms per family including eukaryote-only terms, for a completeness win of only 338 genes (~8%) vs. the 1,311 that justified TCDB's GO bridge — see `plans/backlog.md`. Vocabulary registration: this follow-up adds **5 new** entries (`Gene_has_merops_family.tier`, `Gene_has_merops_family.pfam_support`, `MeropsFamily.cleavage_p1_residues`, `Merops_family_has_pfam_domain.member_id_count`, `Gene.merops_classes`) to `config/controlled_vocabularies.yaml`, completing the merops set of **10** entries — the other 5 (`call_class`, `best_hit_kind`, `level_kind`, `family_type`, `catalytic_type`) were already registered by a parallel change. Built with `/integrate-a-tool` (Phase 2). See `docs/kg-changes/merops-extension.md`.
- SubcellularLocalization nodes (PSORTb): up to 5 flat nodes (the real Gram-negative localizations — `Cytoplasmic`, `CytoplasmicMembrane`, `Periplasmic`, `OuterMembrane`, `Extracellular`; the `Unknown` sentinel is skipped, so absence of an edge encodes "no confident call"). Node IDs use the underscore form `psortb_<Class>` (`psortb` is not a bioregistry prefix, so `normalize_curie` falls back to underscore). Properties: name, psortb_id, level (always 0; flat — no level_kind). Computed post-import: gene_count, organism_count. The edge `Gene_has_subcellular_localization` (Gene → node, 1:1) is the KG's **first scored ontology edge** — carries `score` (float, PSORTb confidence ≈7.5–10.0) + post-import `rank_by_score` (modeled on `Changes_expression_of`, not on any ontology edge). **Structural** (where a gene is, not what it does): deliberately NOT folded into `Gene.annotation_types` / `informative_annotation_types` / `annotation_quality`; instead a denormalized `Gene.subcellular_localization` routing string is set post-import. Merged via the `psortb` source (`psortb_localization` / `psortb_score`, join `protein_id`). Built with `/integrate-a-tool` (Phase 2). See `docs/kg-changes/psortb-extension.md`.
- SignalPeptideType nodes (SignalP 6.0): 5 flat nodes (the real signal-peptide types — `SP` (Sec/SPI), `LIPO` (Sec/SPII), `TAT` (Tat/SPI), `TATLIPO` (Tat/SPII), `PILIN` (Sec/SPIII); the `OTHER` sentinel is skipped, so absence of an edge encodes "no signal peptide"). Node IDs use the underscore form `signalp_<TYPE>` (`signalp` is not a bioregistry prefix, so `normalize_curie` falls back to underscore). Properties: name, signalp_id, level (always 0; flat — no level_kind). Computed post-import: gene_count (SP 9,710 · LIPO 3,065 · TAT 441 · PILIN 314 · TATLIPO 83), organism_count. The edge `Gene_has_signal_peptide_type` (Gene → node, 1:1; ~13.6K edges) is the KG's **second scored ontology edge** (after PSORTb) — carries `probability` (float ∈[0,1], winning-class likelihood), `cleavage_site` (int, residue before the cut; absent when no CS), `cleavage_probability` (float; absent when no CS) + post-import `rank_by_probability` (modeled on `Changes_expression_of`). The full 6-class likelihood vector is intentionally NOT stored. **Structural** (how a gene's product is handled, not what it does): deliberately NOT folded into `Gene.annotation_types` / `informative_annotation_types` / `annotation_quality`; instead a denormalized `Gene.signal_peptide_type` routing string is set post-import. Merged via the `signalp` source (`signalp_type` / `signalp_probability` / `signalp_cleavage_*`, join `protein_id`). `signalp-run --normalize` produces the `<strain>.signalp.calls.json` it reads (the runner predates the calls.json convention). Built with `/integrate-a-tool` (Phase 2). See `docs/kg-changes/signalp-extension.md`.
- InterproEntry nodes (InterProScan 5): ~12,999 nodes (InterPro entries observed on any strain + their is-a ancestors; pruned like TCDB/BRITE). Node IDs use the colon form `interpro:IPR*` (`interpro` **is** a registered bioregistry prefix). Types: FAMILY 6,490 · DOMAIN 4,355 · HOMOLOGOUS_SUPERFAMILY 1,533 · CONSERVED_SITE 390 · ACTIVE_SITE 95 · REPEAT 74 · BINDING_SITE 55 · PTM 7. Properties: name, interpro_id, interpro_type, level (is-a depth in ParentChildTree, 0=parentless root; **sparse** — ~86% are level 0), `description` (str, sparse — **OBSERVED-ONLY mode**: the full 54,068-entry abstract set is 27.1 MB, over the reference's ~25 MB gate, so only the ~13K observed+ancestor kept entries carry one (12,955 non-empty); a brand-new strain's genuinely novel InterPro accessions need `prepare_data.sh --steps 9 --force` re-run after that strain's InterProScan batch before they get a description — see Data Locations). Computed post-import: gene_count (DIRECT — genes with an edge to this exact entry, the correct ORA count), organism_count, member_count (direct child entries). **`is_promiscuous` is DELETED** (vocabulary-contract R3 — it only ever restated `gene_count >= 1000`). A consumer wanting the same 22 ubiquitous entries (e.g. P-loop NTPase) queries the threshold directly: `MATCH (n:InterproEntry) WHERE n.gene_count >= 1000 RETURN n`. The edge `Gene_has_interpro_entry` (Gene → node; ~397K edges; 102,895 genes ~85% covered) is a **scored** ontology edge — carries `start`/`end` (domain envelope), `evalue` (float, nullable — best/min; **omitted** when no match reports one, ~42% of matches carry none), `evalue_library` (str, nullable — which member DB reported that min evalue), `libraries` (str[], member DBs), `match_count` (int). **No cross-library `score`** (2026-08-17 multi-ontology redesign, "count, don't combine" — a synthesized score across heterogeneous scales like Pfam bit-score vs. HAMAP profile-score was never comparable; read `size(libraries)` for cross-library corroboration and `match_count` for total-row count instead). **NO e-value cutoff** — each member DB pre-applies its curated threshold; evalue is evidence-only, never a filter. Hierarchy edge `Interpro_entry_is_a_interpro_entry` (child → parent; ~1,569; is-a only, 100% within-type). Bridge edge `Pfam_in_interpro_entry` (Pfam → node; 5,972) links the eggNOG Pfam layer to InterPro (overlap link, **not** a merge). **Functional** (folded into `Gene.annotation_types` += 'interpro' + `Gene.interpro_entry_count`) but deliberately NOT into `informative_annotation_types` / `annotation_quality` (redundant with pfam/go/ec — conduit signal, see the `ncbifam` bucket note below for the contrast). **ORA over InterPro must stratify by `(interpro_type, level)`** — `interpro_type` PRIMARY (breadth), `level` secondary (the inverse of every other ontology; hierarchy too sparse for level to partition). Merged via the `interproscan` source (`interpro_entries`, join `protein_id`); adapter reads the faceted calls.json directly for edge evidence (`libraries`/`interpro_entries`/`go_terms` facets; no `pathways`, no per-strain `entry_xrefs.json` sidecar — see `.claude/skills/interproscan-run/SKILL.md`). Built with `/integrate-a-tool` (Phase 2). See `docs/kg-changes/interpro-multi-ontology.md` (supersedes `interproscan-extension.md`).
- NcbifamFamily nodes (2026-08-17, split out of the InterPro layer): **4,957** nodes (2,204 `TIGR*` + 2,753 `NF*`), observed-only and flat (no ancestor walk — the source has no parent/child column). Node IDs use the underscore form `ncbifam_<accession>` (`ncbifam` is not a bioregistry prefix). Properties: name, ncbifam_id, family_type (NCBIfam's own classification, **equivalog-dominant** in the observed set — near-EC-grade precision, the reason it earns its own ontology instead of folding into InterPro's broader FAMILY/DOMAIN layer), gene_symbol (sparse), description (sparse, short curated text — NOT observed-only-gated like InterPro's; the full NCBIfam description set fits under the size gate). Computed post-import: gene_count, organism_count. The edge `Gene_has_ncbifam_family` (Gene → node; **67,459** edges; 47,324 genes carry ≥1) carries `start`/`end`/`evalue`/`score`/`match_count` — **keeps both `evalue` AND `score`** (unlike `Gene_has_interpro_entry`), because NCBIfam is one homogeneous HMMER-scale library, not a cross-library rollup. Bridge edge `Ncbifam_family_in_interpro_entry` (NcbifamFamily → InterproEntry; **2,630**) links the two ontologies (overlap, not a merge). `ncbifam` is folded into `Gene.annotation_types` AND `informative_annotation_types`/`annotation_quality` (unlike `interpro`, which stays conduit-only — see the 9-bucket note below) because it is curated and functionally specific. `is_uninformative` flags **195** NcbifamFamily nodes (verified live 2026-08-17: **126** via the `family_type IN ['hypoth_equivalog','hypoth_equivalog_domain']` typed rule, remaining **69** via the `(?i).*hypothetical.*`/`.*uncharacterized.*`/`.*DUF\d.*` name-pattern fallback). Merged via the `interproscan` source's `ncbifam_ids` field (reads the calls.json NCBIFAM facet, join `protein_id`); adapter reads calls.json directly for edge evidence. See `docs/kg-changes/interpro-multi-ontology.md`.
- **InterPro two-layer integration (2026-08-10)** — InterPro's entry-level xrefs now enrich genes AND link ontologies. See `docs/kg-changes/interpro-two-layer.md`.
  - **Layer B (gene enrichment)**: InterPro contributes to `go_terms` (+45K net-new), `ec_numbers` (+9.5K), `cazy_ids` (+642), `pfam_ids` (+14K), noise-gated by entry type — GO/CAZy from FAMILY+DOMAIN (fold excluded), EC from **FAMILY + single-EC only** (multi-EC families → Layer A), Pfam from direct HMM hits. Folded into `Gene.annotation_types` += 'interpro' + `Gene.interpro_entry_count`; NOT yet into `informative_annotation_types`/`annotation_quality` (deferred).
  - **Edge provenance (the six gene→ontology edges: GO×3, `Gene_catalyzes_ec_number`, `Gene_has_pfam`, `Gene_has_cazy_family`)** now carry `sources` (str[], each value joins a `DataSource` node via `id = 'data_source:' + value` — `ncbi|cyanorak|uniprot|eggnog|interproscan`, R2), `evidence` (`curated`>`signature`>`family_inferred`>`domain_inferred` — the field to read for curated-vs-inferred), `evidence_score` (**float in `[0,1]`**, R4; fired signals ÷ total signals, `round`ed to 3 decimals — advisory, never a filter; eggNOG-Pfam & InterPro-Pfam count as ONE source — circularity; `round(score × signal_count)` recovers the raw fired-signal count). Set by `annotation_provenance.annotation_edge_props` from per-token `<field>_source`/`<field>_evidence` maps in `gene_annotations_merged.json` (union `track_source`, previously a no-op, now honoured). Value sets published as `ControlledVocabulary` nodes — see `docs/kg-changes/vocabulary-contract.md`.
  - **Layer A (ontology→ontology ROUTER, NOT annotation)**: `Interpro_entry_related_to_ec_number` (6,854) + `Interpro_entry_related_to_cazy_family` (~122) home the multi-EC/DOMAIN ECs + fold CAZy Layer B refuses. Weak `related_to` verb; the edges carry **no properties at all** — the former `ambiguous` bool (always `false` in every shipped build — an adapter-emitted `bool` never round-trips through BioCypher) and the hardcoded-literal `source_db` (`"interpro.xml"`, never varied) were both **deleted** rather than fixed (vocabulary-contract R3/R5; recover the multiplicity check **approximately** as `MATCH (n:InterproEntry)-[r:...]->() WITH n, count(r) AS k WHERE k > 1 OR n.interpro_type <> 'FAMILY'` — a floor, not a faithful replay, because edges are pruned to gene-referenced ECs after the flag would have been computed: it differs from the old flag on 105 of 5,021 EC-bearing entries (42 read as specific despite competing alternatives no gene here carries; 32 drop a class+member nuance; 31 the derivation gets *right* where the old flag was a false positive from obsolete-EC remapping), see `docs/kg-changes/vocabulary-contract.md`). Recall-biased: read outward it corroborates, read backward (carries EC → is that enzyme) it is low-precision — **never assign gene function from it** (that's `Gene_catalyzes_ec_number`). Dangling-proof by pruning to EC/CAZy nodes the gene edges created (DOMAIN-ECs no gene carries are not materialised — completeness/simplicity tradeoff vs node injection), intersected with the injected `ec_node_ids` — see the EC dangling-proof bullet below.
  - **EC dangling-proof (2026-08-12)**: a gene's `ec_numbers` can name an EC that Expasy has **no node for**, so `Gene_catalyzes_ec_number` needs the same pruning as everything else. InterPro's entry-level EC xrefs carry obsolete numbers (`1.2.8.1`, deleted before Expasy's transfer map covers it) and outright invalid ones (`2.8.3.183`, from IPR017821 — Expasy's `2.8.3.*` stops at `.28`); `normalize_ec` remaps neither, so Layer B propagated them onto 5 genes. `MultiEcAnnotationAdapter.get_edges` now prunes gene→EC edges to `all_ec_node_ids()` (the Expasy node universe, 7,337 ids) and logs what it drops; `create_knowledge_graph` injects that same set into `MultiInterproAnnotationAdapter(ec_node_ids=…)` so Layer A intersects with it (`None` → emit no Layer-A EC edges, mirroring `pfam_node_ids`). Removed the last 9 dangling edges in `import.report`; `Gene_catalyzes_ec_number` stays at 69,026 and Layer A at 6,854 distinct, since `neo4j-admin import` was already skipping them. Layer A emits ~103 duplicate edge ids (two raw tokens on one entry normalising to the same EC) that dedup at import — harmless, pre-existing.
  - InterPro ships **no KEGG** pathway xrefs (MetaCyc + species-collapsed Reactome only) — KEGG pathways stay KO-derived. GO from InterPro is entry-level (inline `go_terms` facet in the faceted calls.json / the `interpro_reference.json` reference — the old per-strain `entry_xrefs.json` sidecar this was originally written against no longer exists, see the 2026-08-17 multi-ontology redesign), reproducing per-protein scan GO 100/100. **MCP not yet updated** — source/evidence filters + 2-hop router mode are a follow-up (spec §7).
- **TCDB cross-ontology bridges** (ontology→ontology only — they never touch Gene nodes): `Tcdb_family_has_pfam_domain` (TcdbFamily → Pfam, **1,856**), `Tcdb_family_involved_in_biological_process` (**2,588**), `Tcdb_family_enables_molecular_function` (**2,266**), `Tcdb_family_located_in_cellular_component` (**1,708**). Built from TCDB's published Pfam→TC and GO→TC maps (downloaded by step 6 into `cache/data/tcdb/raw/tcdb_{pfam,go}_map.tsv`), rolled onto the pruned node set by `build_tcdb_bridges` — same shape as `subtree_substrates`. Targets shallower than `tc_family` (level < 2) are dropped as uninformative. Each edge carries `curated_tcids: str[]`, the original published 5-part TCIDs, so roll-up loses no precision. Dangling-proof: `create_knowledge_graph` injects `pfam_node_ids` + `go_terms` (BRITE-`known_ko_ids` precedent); without them **no** bridge edges are emitted.
  - **Semantics — direction is load-bearing.** These assert *"TCDB's curated reference proteins for this transport family carry this domain / GO annotation"* — a statement about the **composition** of the TC family. Measured over 42 strains: read outward from a gene's known TC family, the Pfam bridge **corroborates 85%** of calls and contradicts 2%. Read **backwards** (carries the domain → therefore a transporter) it is only **~31% precise**, because these domains also occur outside transport contexts (response-regulator receiver, histidine kinase, TPR and GGDEF domains all map to TC families via accessory proteins of curated systems). **Never use these edges to assign transporter identity to a gene** — that is what `Gene_has_tcdb_family` is for. A `gene → InterproEntry → Pfam → TcdbFamily` traversal is a recall-biased router (like `Publication_discusses_gene`), not an annotation.
  - The GO bridge is the completeness win: via the sound 2-hop `gene → TcdbFamily → GO` (first hop is direct evidence), 1,311 genes with no GO at all gain a specific (level ≥4) term.
- **`Gene_has_tcdb_family` carries TWO evidence sources** — eggNOG's `KEGG_TC` (ortholog transfer) and `/tcdb-diamond` (direct blastp vs. the curated TCDB FASTA). Both emit the SAME vocabulary, so there is **one ontology and one edge per (gene, TC id)**, with provenance on the edge: `sources: str[]` (sorted; each value joins a `DataSource` node via `id = 'data_source:' + value` — `'tcdb_diamond'` | `'eggnog'`, R2). Prefer `'eggnog' IN r.sources` over list equality. A TC called by both yields ONE edge with two entries — that agreement is the cross-source corroboration signal. Diamond evidence rides sparsely on edges diamond called: `tier` (int 1-3), `confidence_score`, `identity`, `qcov`, `evalue` (floats), `consensus_n` (int). Merged via the `tcdb_diamond` source: `transporter_classification` is the UNION of both (this is what step 6 seeds TCDB pruning from), with `tcdb_eggnog_ids` / `tcdb_diamond_ids` preserving attribution; `tcdb_adapter` reads the calls.json directly for per-call evidence (interpro_adapter pattern).
  - **Diamond is a BREADTH source, not a depth source** — contrary to the Phase-1 spec's premise. eggNOG is already deeper (240 distinct 5-part ids vs diamond's 36). Diamond's win: 15,074 proteins get a call with no eggNOG TC, plus 762 TC ids eggNOG never mentions.
  - **Tier gate.** `tier` is both a confidence band and the truncation depth (1 = `tc_specificity`, identity ≥70; 2 = `tc_subfamily`, ≥40; 3 = `tc_family`, no identity floor). Tier 3 dominates but is conservative-by-design remote homology (median identity 34%, median e-value 2.3e-30), so ALL candidates become edges. Post-import folds `'tcdb'` into `annotation_types` / `informative_annotation_types` / the `annotation_quality` bucket **only** for eggNOG-sourced or tier ≤2 edges — tier-3 calls stay findable without inflating a signal calibrated on curated sources. `Gene.tcdb_family_count` / `Gene.metabolite_count` are NOT gated (routing counts, not quality).
  - **`evidence_score` (float `[0,1]`, post-import; renamed from `tcdb_evidence_score`, R4)** — an **advisory** ranking over five independent signals, never a filter: `+1` eggNOG called it · `+1` `source_agreement = 'both_sources'` · `+1` `tier <= 2` · `+1` `pfam_support = 'corroborated'` · `+1` `go_support = 'corroborated'`; fired signals ÷ 5, rounded to 3 decimals. `round(score × 5)` recovers the raw fired-signal count. The three underlying facts are stored **alongside** the total, as R5-compliant categorical strings rather than native booleans (`source_agreement: both_sources | single_source`, `pfam_support: corroborated | uncorroborated`, `go_support: corroborated | uncorroborated`), so consumers can see why and re-weight without re-deriving. `Gene.tcdb_evidence_score_max` (renamed from `tcdb_best_evidence_score`, also float `[0,1]`) = `max(evidence_score)` over a gene's edges; sparse (absent when the gene has no TCDB edge) — use `coalesce(g.tcdb_evidence_score_max, -1.0)` for a total order, not `-1` (0.0 is now a legitimate value).
    - **Agreement is HIERARCHICAL, not exact-node.** eggNOG names subfamilies while diamond's tier-3 truncation names the parent family, so the sources usually concur at *different depths*: exact same-node agreement covers only 3,641 edges, while including ancestor/descendant agreement reaches 21,684 (40.3%). Scoring on `size(sources)=2` alone would miss 83% of real corroboration.
    - Pfam and GO are separate components because they are **not redundant** — 11,565 edges carry exactly one of the two (P(GO|Pfam) = 77% vs P(GO|¬Pfam) = 20%).
    - Corroboration is read in the **sound** direction only (gene's TC family known → does Pfam/GO agree?), which measures 85% corroborate / 2% contradict. The reverse reading is ~31% precise — see the `Tcdb_family_has_pfam_domain` note.
    - See `docs/kg-changes/vocabulary-contract.md` for the full rename table and `docs/kg-changes/tcdb-two-source-upgrade.md` §7.1 for the related chemistry-count definitions.
  - There is no `filter_action` on the Phase-1 artifact — the old 5-rule chain was removed because its verdicts depended on unrelated sibling candidates.
- Five ontology edges: Gene_has_tcdb_family (Gene → TcdbFamily, attaches at exact annotated level), Tcdb_family_is_a_tcdb_family (child → parent), Tcdb_family_transports_metabolite (TcdbFamily → Metabolite, **pre-rolled-up at step-6 time over the FULL TCDB hierarchy** — every kept ancestor carries the union of substrate primary IDs from every tc_specificity descendant in TCDB, not just gene-annotated ones; tcdb_pruned.json carries `subtree_substrates: {tc_id: [primary_id, ...]}` and the adapter just emits one edge per pair. Flattens the descendants walk into edges so post-import gene/organism rollups become single-hop at any level; recover leaf-only semantics via `WHERE source.level_kind = 'tc_specificity'`), Gene_has_cazy_family (most specific observed level), Cazy_family_is_a_cazy_family.
- Metabolite.evidence_sources (str[]): values 'metabolism' (Reaction-reachable) and 'transport' (TCDB-substrate-reachable). Open-ended; 'metabolomics' reserved for a future metabolomics-DM spec.
- Metabolite.transporter_count (int, post-import): distinct transporter systems at **maximal depth** — sources of `Tcdb_family_transports_metabolite` edges whose `substrate_depth = 'most_specific'`, so an ancestor is never counted alongside its own descendant. Non-zero for all 1,462 transported metabolites (median 1, max 123). **Was** `level_kind = 'tc_specificity'`-only, which read 0 for 1,218 of them (83%).
- **Substrate-rollup depth (2026-08-12, values renamed 2026-08-18 per vocabulary-contract R5)** — `Tcdb_family_transports_metabolite` carries `substrate_depth: str` (`'most_specific'` | `'inherited'` — was `'deepest'` | `'ancestor'`). `'most_specific'` = no kept CHILD of this node carries the same substrate, i.e. this is the most specific surviving transporter system for it. It is a **(node, substrate) fact, not a node fact** — `2.A.1` can be `most_specific` for one substrate and `inherited` for another. Set by `tcdb_adapter._compute_substrate_depth` from `tcdb_pruned.json`; checking DIRECT children suffices because `_prune_tcdb` only walks up, so the kept set is ancestor-closed. Categorical **string**, not bool — BioCypher mishandles boolean properties. NOT the same as curated-vs-inherited: only `tc_specificity` nodes own `substrate_classes` and they are leaves, so that distinction is already exactly `level_kind = 'tc_specificity'`.
- **Metabolite / Gene / Organism metabolite counts are split by evidence arm (BREAKING, 2026-08-12).** `metabolite_count` used to union catalysis with transport; the arms are not comparable (catalysis p90 = 11, transport p90 = 554, because the step-6 rollup materialises every descendant's substrates onto each ancestor) and 23,137 genes had transport evidence ONLY, so for most genes the stored number was entirely the inflated arm.
  - `Gene.metabolite_count` = catalysis only (27,340 genes, p90 11). `Gene.transported_metabolite_count` = transport (26,813 genes, p90 97).
  - `Metabolite.gene_count` = catalysis only. `Metabolite.transporter_gene_count` = transport.
  - `OrganismTaxon.metabolite_count` = `'metabolism'` arm. `OrganismTaxon.transported_metabolite_count` = `'transport'` arm. (`measured_metabolite_count` already existed — this completes the three-way split.)
  - The transport arm everywhere counts each gene's **DEEPEST TC attachments only** (`NOT EXISTS` a descendant attachment, `*1..4`). 6,950 genes are annotated at both an ancestor and its own descendant (e.g. `3.A.1` *and* `3.A.1.14`) and were inheriting the superfamily's full rollup regardless; restricting to deepest keeps 99.7% of genes and takes p90 from 554 → 97. Both ends use the same predicate, so `Gene.transported_metabolite_count` and `Metabolite.transporter_gene_count` are two projections of one (gene, metabolite) set and agree by construction.
- **Gene.transport_substrate_resolution** (str, post-import, **sparse**): `'resolved'` (28,405 genes, p90 33) | `'family_inferred'` (1,671, p90 554). `family_inferred` = the gene's deepest TC attachment is a substrate-lumping node (`level >= 2 AND metabolite_count >= 50`, e.g. ABC superfamily `3.A.1` with 554 substrates) — the count is reachability, not capability; take the substrate from `product` / COG / `function_description` instead. Absent when the gene has no TCDB edge, so "no transporter evidence" stays distinguishable from a weak substrate claim. **Deliberately NOT tier-gated** — tier and substrate resolution are orthogonal: 11,871 `resolved` genes are tier-3-only (narrow `2.A.x` secondary carriers where remote homology could not justify a subfamily call, exactly the ones TCDB resolves well), and a tier gate would discard them while keeping eggNOG's equally-lumping `tc_family` edges since eggNOG carries no tier. Tier keeps its existing homes: the edge property, the `annotation_types`/`annotation_quality` gate, and `Gene.tcdb_evidence_score_max`.
- Gene.tcdb_family_count / cazy_family_count / merops_family_count: distinct ontology edges per gene (post-import; merops also gets `merops_classes` str[]). Gene.annotation_types extended with 'tcdb' / 'cazy' / 'merops' (tcdb + merops tier-gated).
- Organism_has_metabolite materialization: includes the transport arm (Gene → deepest TcdbFamily attachments → Metabolite). Single MERGE deduplicates by (organism, metabolite). Previously one gene annotated at `3.A.1` gave its whole organism all 554 ABC substrates — which is how every organism came to "have" ~63% of all metabolite nodes.
- Removed Gene properties (replaced by graph edges): transporter_classification (use Gene_has_tcdb_family), cazy_ids (use Gene_has_cazy_family). Also removed: build-time `_compute_annotation_quality` heuristic (annotation_quality now computed entirely in post-import Cypher; see annotation_state/annotation_quality bullets).
- Reaction nodes (~3K) with primary ID `kegg.reaction:R*`. Properties: kegg_reaction_id, name, ec_numbers, kegg_pathway_ids, mnxr_id (nullable), rhea_ids, reaction_class ('transport'|'chemical'), mass_balance ('balanced'|'unbalanced'). Computed post-import: gene_count, organism_count, organisms[]. KEGG-native primary IDs (Spec 1.2 pivot from MNX-as-primary).
- Metabolite nodes (~1.5K) with primary ID `kegg.compound:C*` (with chebi: / mnx: fallback for compounds without KEGG entry). Properties: kegg_compound_id, name, formula, elements (str[], sparse — sorted unique element symbols Hill-parsed from formula via chemparse; absent when formula is null or contains no element symbols), mass, inchikey, smiles, mnxm_id (nullable), chebi_id (nullable), hmdb_id (nullable). Computed post-import: gene_count, organism_count, transporter_count, pathway_ids (str[], sorted distinct KeggTerm.id reachable via Metabolite_in_pathway), pathway_names (str[], aligned index-for-index with pathway_ids), pathway_count (int, = size(pathway_ids)), organism_names (str[], sorted distinct OrganismTaxon.preferred_name reachable via Organism_has_metabolite; size invariant: == organism_count).
- `Gene_catalyzes_reaction` edges from gene["kegg_reactions"]; `Reaction_has_metabolite` edges from KEGG /link/compound/reaction (direction-agnostic — Spec 1.2 explicitly does NOT model reaction direction); `Reaction_in_kegg_pathway` edges reuse existing KeggTerm pathway nodes (kegg.pathway:ko*); `Metabolite_in_pathway` edges (Metabolite → KeggTerm pathway) materialize compound→pathway annotations from KEGG, pruned to KO ∪ Reaction-reachable pathways (Option B; drops compound-only pathways like Leishmaniasis or MAPK signalling that are biologically inappropriate for marine bacteria); `Organism_has_metabolite` edges materialized post-import as a 2-hop save (Gene→Reaction→Metabolite, distinct).
- MetaboliteAssay nodes (Phase 2 metabolomics): one per (Experiment × value_kind). Adapter: `metabolite_assay_adapter.py` (mirrors `observations_adapter.py`). Properties match DerivedMetric pattern (denormalized parent-Experiment block: `experiment_id`, `organism_name`, `publication_doi`, `omics_type`, `treatment_type`, `background_factors`, `treatment`, `light_condition`, `experimental_context`, `compartment` + per-assay: `metric_type`, `value_kind ∈ {numeric,boolean}`, `unit`, `rankable`, `aggregation_method ∈ {mean_across_replicates, pre_aggregated, single_measurement}`, `field_description`). Node IDs: `metabolite_assay:{doi_short}:{entry_key}:{metric_type}`. Edges: `Assay_quantifies_metabolite` (numeric, replicate-aggregated; carries `value`, `value_sd`, `n_replicates`, `n_non_zero`, `replicate_values`, adapter-set `detection_status ∈ {detected, sporadic, not_detected}`; rank/percentile/bucket post-import on `rankable="true"`) + `Assay_flags_metabolite` (boolean presence; carries `flag_value`, `n_replicates`, `n_positive`). Bindings: `PublicationHasMetaboliteAssay`, `ExperimentHasMetaboliteAssay`, `MetaboliteAssayBelongsToOrganism`. Phase 2 adds `extracellular` to the compartment vocab (in addition to existing `whole_cell, vesicle, exoproteome, spent_medium, lysate`).
- MetaboliteAssay computed properties (post-import): `total_metabolite_count` (int), `value_min`/`value_max`/`value_q1`/`value_median`/`value_q3` (float, numeric assays only), `flag_true_count`/`flag_false_count` (int, boolean assays only), `growth_phases` (str[] from parent Experiment). Numeric edge computed properties (rankable=true only): `rank_by_metric` (1 = highest value), `metric_percentile` (0-100), `metric_bucket` (`top_decile`/`top_quartile`/`mid`/`low`).
- `Organism_has_metabolite` extends to a 3-source UNION (catalysis ∪ transport ∪ measurement). Post-import properties: `evidence_sources` (str[], values `metabolism` | `transport` | `measured`), `measured_assay_count`, `measured_compartments`, `measured_paper_count` (0/[]/0 on non-measured edges).
- Metabolite node gains post-import `measured_assay_count`, `measured_organisms` (str[]), `measured_compartments` (str[], sorted distinct compartments across incoming Assay edges; `[]` on non-measured), `measured_paper_count`. `evidence_sources` may include `"metabolomics"`. `organism_count`/`organism_names` UNION extended to include the measurement path.
- Experiment + Publication gain `metabolite_assay_count`, `metabolite_compartments` (str[]), `metabolite_count` (post-import). OrganismTaxon gains `measured_metabolite_count`.
- See `docs/kg-changes/metabolomics-extension.md`.
- BriteCategory nodes: ~2,560 nodes from 12 KEGG BRITE functional hierarchy trees. Node IDs: `kegg.brite:{tree_code}.{A#}.{B#}...` (positional, 1-based, no tree-root node emitted). Properties: `name` (str), `tree` (snake_case tree name, e.g. `"transporters"`), `tree_code` (e.g. `"ko02000"`), `level` (int, 0=broadest/A-level=brite_class, 1=brite_subclass, 2=brite_family, 3=brite_subfamily), `level_kind` (str). Computed post-import: `member_ko_count` (int, recursive KO leaf count), `gene_count` (int, distinct genes reachable via KO leaves), `organism_count` (int). 12 trees: enzymes (ko01000), transporters (ko02000), peptidases (ko01002), transcription_factors (ko03000), secretion (ko02044), two_component (ko02022), defense (ko02048), chaperones (ko03110), ribosome (ko03011), translation_factors (ko03012), trna_biogenesis (ko03016), dna_replication (ko03032). Edges: ~2,500 `Brite_category_is_a_brite_category` (child→parent hierarchy within each tree) + ~4,100 `Kegg_term_in_brite_category` (KeggTerm KO → BriteCategory leaf). KO nodes connect to their immediate BRITE parent category. A-level entries are level=0 with no parent edge. BRITE output is pruned to the subhierarchy reachable from `KeggTerm` nodes — a `BriteCategory` node appears iff at least one KO in its subtree is referenced by a gene in the KG. Pruning is keyed off `MultiKeggAnnotationAdapter.all_ko_ids()` (see `docs/superpowers/specs/2026-04-14-brite-pruning-design.md`).
- ClusteringAnalysis nodes: intermediate layer between Publication and GeneCluster. Each groups clusters from one clustering analysis entry in a paperconfig. Properties: `name`, `organism_name`, `cluster_method`, `cluster_type`, `cluster_count`, `total_gene_count`, `omics_type`, `treatment_type` (str[]), `background_factors` (str[]), `treatment`, `light_condition`, `experimental_context`. Node IDs: `clustering_analysis:{doi_short}:{entry_key}`. Linked via `Publication_has_clustering_analysis` (Publication → ClusteringAnalysis), `Clusteringanalysis_belongs_to_organism` (ClusteringAnalysis → OrganismTaxon), and `Experiment_has_clustering_analysis` (Experiment → ClusteringAnalysis).
- GeneCluster nodes: one per unique cluster value in the CSV. Node IDs: `cluster:{doi_short}:{analysis_entry_key}:{csv_cluster_key}`. Properties: `id` (extracted short identifier), `name`, `organism_name`, `member_count`, `functional_description`, `temporal_pattern`, `expression_dynamics`. Per-cluster descriptions (`id`, `name`, `functional_description`, `temporal_pattern`, `expression_dynamics`) come from extraction JSON files (`cluster_extraction_{entry_key}.json`); only applied when stage3 validation verdict = "pass". Experimental metadata (cluster_method, cluster_type, treatment_type, omics_type, etc.) lives on the parent ClusteringAnalysis node, not on GeneCluster. Linked to analysis via `Clustering_analysis_has_gene_cluster`; linked to genes via `Gene_in_gene_cluster`.
- DerivedMetric nodes: 13 (Biller 2018: 6 boolean + 1 categorical; Waldbauer 2012: 6 numeric). Node IDs: `derived_metric:{doi_short}:{entry_key}:{metric_type}`. Properties: `name`, `metric_type`, `value_kind ∈ {numeric, boolean, categorical}`, `rankable` (str `"true"`/`"false"`), `has_p_value` (str `"true"`/`"false"`), `p_value_threshold` (float, nullable), `unit`, `allowed_categories` (str[]), `field_description`, `compartment`, plus denormalized parent-Experiment fields (`experiment_id`, `organism_name`, `publication_doi`, `omics_type`, `treatment_type` (str[]), `background_factors` (str[]), `treatment`, `light_condition`, `experimental_context`). Each DM emits exactly one of 3 measurement edge types: `Derived_metric_quantifies_gene` (numeric — 1,872 edges in Waldbauer 2012, 312 genes × 6 metrics), `Derived_metric_flags_gene` (boolean — 4,160 edges in Biller 2018), `Derived_metric_classifies_gene` (categorical — 258 edges in Biller 2018). Binding edges: `PublicationHasDerivedMetric`, `ExperimentHasDerivedMetric`, `DerivedMetricBelongsToOrganism`. See `docs/kg-changes/non-de-evidence-extension.md`.
- DerivedMetric computed properties (post-import): `total_gene_count` (int), `growth_phases` (str[] from parent Experiment).
- Numeric DM edge computed properties (post-import, only when parent `rankable="true"`): `rank_by_metric` (int, 1 = highest value), `metric_percentile` (float 0-100), `metric_bucket` (str: `top_decile`/`top_quartile`/`mid`/`low`, pinned thresholds ≥90/≥75/≥25). When parent `has_p_value="true"` AND threshold is set AND `adjusted_p_value` non-null: `significant` (str `"true"`/`"false"`).
- Experiment `compartment` property (str, adapter-emitted, default `"whole_cell"`). Vocab: `whole_cell`, `vesicle`, `exoproteome`, `secretome`. Computed Experiment DM rollups (post-import): `reports_fold_change` (str `"true"`/`"false"`), `reports_derived_metric_types` (str[]), `derived_metric_count` (int), `derived_metric_value_kinds` (str[]), `derived_metric_gene_count` (int), `derived_metric_search_text` (str, null on experiments with no DMs — aggregated DM `name` + tokenized `metric_type` + `field_description`; consumed by `experimentFullText`).
- Publication/OrganismTaxon DM rollups (post-import): `derived_metric_count` (int), `derived_metric_gene_count` (int), `compartments` (str[]), `derived_metric_types` (str[]), `derived_metric_value_kinds` (str[]). Publication also gets `derived_metric_search_text` (str, null on publications with no DMs — same aggregation as Experiment; consumed by `publicationFullText`).
- Gene DM routing signals (post-import): `numeric_metric_count`, `boolean_metric_count`, `categorical_metric_count` (ints, default 0); `numeric_metric_types_observed`, `boolean_metric_types_observed`, `categorical_metric_types_observed` (str[], default []); `compartments_observed` (str[], default []).
- EnvironmentalCondition nodes no longer exist — replaced by Experiment nodes (see above)
- `preferred_name` property on `OrganismTaxon` nodes (e.g., `"Prochlorococcus MED4"`)
- `organism_type` property on `OrganismTaxon` nodes: `"genome_strain"` (25 organisms — real genome assemblies from cultured isolates), `"treatment"` (5 organisms — non-genomic coculture partners), `"reference_proteome_match"` (2 organisms — identified by matching experimental data against a multi-organism reference DB). Reference proteome match organisms also carry `reference_database` (e.g., `"MarRef v6"`) and `reference_proteome` (assembly accession). See `docs/kg-changes/reference-proteome-match-organisms.md`.
- OrganismTaxon computed properties (post-import): `gene_count` (int), `publication_count` (int), `experiment_count` (int), `treatment_types` (str[]), `omics_types` (str[]), `background_factors` (str[]), `metabolite_count` (int), `reaction_count` (int). Computed after Publication.organisms alignment. `species` is derived from `preferred_name` when NCBI taxonomy is genus-level only (e.g., Alteromonas strains → "Alteromonas macleodii").
- Gene node computed fields for MCP gene lookup: `organism_name` (preferred organism name), `gene_summary` ("name :: product :: description"), `all_identifiers` (union of all alt IDs)
- Gene `annotation_quality` (post-import, refined from build-time): numeric encoding of `annotation_state` (0=no_evidence, 1=catch_all_only, 2=informative_single, 3=informative_multi), driven by `informative_source_count` over 9 source buckets (as of 2026-08-17, up from 8): `go`, `kegg`, `pfam`, `ec`, `role` (via gene_category), `reaction`, `transporter` (via Gene_has_tcdb_family), `cazy` (via Gene_has_cazy_family), `ncbifam` (via Gene_has_ncbifam_family). See `docs/superpowers/specs/2026-05-01-explorer-frictions-resolution-design.md` for maintenance procedure.
- Gene `informative_annotation_types: str[]` (post-import): granular per-source list parallel to legacy `annotation_types` but with the term-informativeness filter applied. 14 sources (as of 2026-08-17, up from 13): `go_bp`, `go_mf`, `go_cc`, `pfam`, `cog_category`, `kegg`, `brite`, `ec`, `cyanorak_role`, `tigr_role`, `reaction`, `transporter`, `cazy`, `ncbifam`. The last four are exclusive to `informative_annotation_types` (not in legacy `annotation_types`) — except `ncbifam`, which (unlike `interpro`) IS also in legacy `annotation_types` (see the NcbifamFamily bullet above). Empty when `annotation_state = 'no_evidence'`.
- Gene `tcdb_evidence_score_max` (float `[0,1]`, post-import): the strongest TCDB claim a gene has = `max(evidence_score)` across its `Gene_has_tcdb_family` edges. See the full description under the "carries TWO evidence sources" bullet above (R4 rename from `tcdb_best_evidence_score`, an int 0-5 scale).
- Gene `contributing_sources: str[]` (build-time, set by `_compute_contributing_sources`): per-gene list of data-source labels that contributed at least one field. Values from `{ncbi, cyanorak, eggnog, uniprot, psortb, signalp, interproscan, tcdb_diamond}`. Distinguishes "ncbi present, others didn't run/match" from "no data" cleanly. 8 corresponding `DataSource` nodes carry source metadata (scope, provenance, info_types).
- DataSource nodes: 9 nodes derived from `config/gene_annotations_config.yaml`'s `logical_sources` block — `ncbi`, `cyanorak`, `eggnog`, `uniprot`, `psortb`, `signalp`, `interproscan`, `tcdb_diamond`, `merops_diamond`. Node IDs use `data_source:` prefix (e.g., `data_source:cyanorak`). Properties: `description`, `info_types` (str[], e.g., `["gene_coordinates", "product", "go_terms"]`), `scope` (`global` | `organism_restricted`), `provenance` (`reference_db` | `tool_run`), `applies_to_organisms` (str[], populated only when `scope='organism_restricted'`). Currently only `cyanorak` is organism-restricted (Pro/Syn strains).
- **Source bucket maintenance**: the `annotation_quality` / `annotation_state` source-bucket list is **explicitly enumerated**, not auto-discovered. The 9 live buckets (as of 2026-08-17, up from 8): `go`, `kegg`, `pfam`, `ec`, `role`, `reaction`, `transporter`, `cazy`, `ncbifam`. **Not** `interpro` — InterPro stays a conduit/routing signal only (folded into `annotation_types` but not this bucket list, since it's largely redundant with `pfam`/`go`/`ec`); NCBIfam earned its own bucket because it's curated and functionally specific (see the NcbifamFamily bullet above). The `has_any_edge` predicate that backs `annotation_state = 'no_evidence'` was separately fixed (2026-08-17) to include `Gene_has_interpro_entry`/`Gene_has_ncbifam_family` in its relationship-type list — previously a gene whose *only* annotation was InterPro/NCBIfam was miscounted as `no_evidence` instead of `catch_all_only` (measured impact: −420 `no_evidence` / +236 net `catch_all_only` on the 2026-08-17 rebuild — see `docs/kg-changes/interpro-multi-ontology.md`). To add a new bucket, follow the procedure in `docs/superpowers/specs/2026-05-01-explorer-frictions-resolution-design.md` — touches the post-import Cypher (both `.cypher` and `.sh`), this CLAUDE.md, and the bucket-count test (`tests/test_annotation_quality_buckets.py` — a real static gate as of 2026-08-17; earlier references to "the bucket-count test" in this doc predated its actual creation). The 14-source `informative_annotation_types` is independently maintained but coarsens to the same 9 buckets (`go = go_bp ∪ go_mf ∪ go_cc`, `role = cog_category ∪ cyanorak_role ∪ tigr_role`, `brite` extra).
- Gene nodes carry ~28 properties. Kept properties: core (locus_tag, start, end, strand, product, protein_id, contig, sequence), naming (gene_name, gene_name_synonyms, alternate_functional_descriptions, function_description), function (protein_family, catalytic_activities, transmembrane_regions, signal_peptide, bigg_reaction, seed_ortholog, seed_ortholog_evalue), computed (organism_name, gene_summary, all_identifiers, contributing_sources), quality (annotation_quality, annotation_state, gene_category), routing signals (annotation_types, informative_annotation_types, expression_edge_count, significant_up_count, significant_down_count, reaction_count, metabolite_count, tcdb_family_count, cazy_family_count, closest_ortholog_group_size, closest_ortholog_genera, cluster_membership_count, cluster_types — set by post-import Cypher, not adapter). reaction_count is the count of Gene_catalyzes_reaction edges; metabolite_count is the distinct count of Metabolite nodes reachable via any gene-reaching path (UNION across catalysis + transport, per TCDB-S3 / KG-A2). `sequence` is the AA sequence string joined from the per-strain NCBI `protein.faa` via RefSeq `protein_id` (adapter-set in `cyanorak_ncbi_adapter`); present on ~96.8% of genes (~116.5K / 120.4K; absent on pseudogenes / locus tags without a protein_id), avg ~305 aa. **Not indexed** (absent from `geneFullText`, no btree index) and **not in any informativeness/routing signal** — it is a retrieval-only payload field, so avoid `RETURN g` (whole-node) or returning it in MCP/LLM responses unless explicitly requested. Redundant ID arrays removed: `gene_synonyms` (= alternative_locus_tags ∪ gene_name_synonyms), `alternative_locus_tags` (⊂ all_identifiers), `old_locus_tags` (⊂ alternative_locus_tags ⊂ all_identifiers). See `docs/gene_id_fields_spec.md` for full analysis.
- Publication computed properties (post-import): `experiment_count` (int), `treatment_types` (str[]), `omics_types` (str[]), `background_factors` (str[]), `organisms` (str[] — sorted distinct `organism_name` + `coculture_partner` from experiments). The adapter no longer sets organism; it is fully computed post-import as `organisms`.
- **Publication "discusses" edges** (literature index, distinct from the supp-table expression data): `Publication_discusses_gene` (Publication → Gene) and `Publication_discusses_kegg_pathway` (Publication → KeggTerm). Capture genes/pathways a paper discusses in its *narrative* (regulators, model genes, pathways named in prose) — NOT just the DE tables. Built by `publication_topics_adapter` from `publication_topics/topics_resolved.json`. Edge props: `prominence` (`central`|`peripheral`), `evidence` (extraction quote). The edges are a recall-biased **router** ("which papers discuss gene/pathway X?"), best-effort not exhaustive; unresolved mentions are dropped. Three-stage pipeline (extract → resolve → adapter): (1) `/extract-discussed-topics` skill / `python -m multiomics_kg.extraction.topics.extract` (full PDF → LLM, 15-pp chunks, refs skipped) → `topics.json`; (2) `python -m multiomics_kg.download.resolve_paper_topics` resolves genes via per-strain `gene_id_mapping` (identifiers-first, strain-aware, gene families fan out per member; `all`/`unspecified` → one edge per paper strain) + pathways via the global `kegg_data.json` lookup (dangling-proof) → `topics_resolved.json` + `resolution_report.txt`; (3) the adapter emits edges. Multi-strain: a gene attributed to one strain resolves there; an `all` mention resolves in each paper strain (bounded to the paper's strains). Post-import rollups: `Publication.discussed_gene_count`/`discussed_pathway_count`, `Gene.discussed_in_publication_count`. See `plans/publication_discusses_edges.md`.
- Post-import indexes: scalar (`gene_locus_tag_idx`, `gene_name_idx`, `gene_organism_name_idx`, `gene_org_contig_start_idx` (composite RANGE `Gene(organism_name, contig, start)` — backs the `gene_neighbors` genomic-window seek: equality prefix `(organism_name, contig)` + ordered range suffix `start`, replacing a full-organism scan + in-memory sort), `ortholog_group_id_idx`, `ortholog_group_name_idx`, `ortholog_group_level_idx`, `ortholog_group_rank_idx`, `pfam_name_idx`, `pfam_clan_name_idx`, `tcdb_family_level_idx`, `tcdb_family_level_kind_idx`, `tcdb_family_tcdb_id_idx`, `tcdb_family_tc_class_id_idx`, `cazy_family_level_idx`, `cazy_family_level_kind_idx`, `cazy_family_cazy_id_idx`, `interpro_entry_level_idx`, `interpro_entry_type_idx`, `interpro_entry_id_idx`, `ncbifam_family_id_idx`, `ncbifam_family_type_idx`, `ncbifam_family_level_idx`, `merops_family_level_idx`, `merops_family_level_kind_idx`, `merops_family_id_idx`, `subcellular_localization_level_idx`, `subcellular_localization_id_idx`, `signal_peptide_type_level_idx`, `signal_peptide_type_id_idx`, `brite_category_tree_idx`, `brite_category_level_idx`, `brite_category_name_idx`, `experiment_id_idx`, `experiment_organism_idx`, `experiment_treatment_type_idx`, `experiment_omics_type_idx`, `experiment_background_factors_idx`, `experiment_compartment_idx`, `clustering_analysis_organism_idx`, `clustering_analysis_method_idx`, `clustering_analysis_type_idx`, `derived_metric_metric_type_idx`, `derived_metric_value_kind_idx`, `derived_metric_compartment_idx`, `derived_metric_omics_type_idx`, `derived_metric_treatment_type_idx`, `derived_metric_organism_idx`, `derived_metric_experiment_idx`, `reaction_id_idx`, `reaction_kegg_id_idx`, `reaction_mnxr_idx`, `metabolite_id_idx`, `metabolite_kegg_id_idx`, `metabolite_mnxm_idx`, `metabolite_chebi_idx`, `metabolite_assay_organism_idx`, `metabolite_assay_compartment_idx`, `metabolite_assay_metric_type_idx`, `metabolite_assay_value_kind_idx`, `metabolite_assay_experiment_idx`) + full-text (`geneFullText` on gene_summary, all_identifiers, gene_name_synonyms, alternate_functional_descriptions; `orthologGroupFullText` on OrthologGroup consensus_product, consensus_gene_name, description, functional_description; `pfamFullText` on Pfam name, short_name; `pfamClanFullText` on PfamClan name; `tcdbFamilyFullText` on TcdbFamily name, tcdb_id, superfamily, description (2026-08-18, drop+recreate); `cazyFamilyFullText` on CazyFamily name; `interproEntryFullText` on InterproEntry name, description; `ncbifamFamilyFullText` on NcbifamFamily name, gene_symbol, description; `meropsFamilyFullText` on MeropsFamily name, merops_id, description, cleavage_summary (2026-08-18, drop+recreate); `subcellularLocalizationFullText` on SubcellularLocalization name, psortb_id; `signalPeptideTypeFullText` on SignalPeptideType name, signalp_id; `briteCategoryFullText` on BriteCategory name; `experimentFullText` on Experiment name, treatment, control, experimental_context, light_condition, compartment, derived_metric_search_text; `publicationFullText` on Publication title, abstract, description, compartments, derived_metric_search_text; `clusteringAnalysisFullText` on ClusteringAnalysis name, treatment, experimental_context; `derivedMetricFullText` on DerivedMetric name, field_description; `reactionFullText` on Reaction name; `metaboliteFullText` on Metabolite name, formula; `metaboliteAssayFullText` on MetaboliteAssay name, field_description, treatment, experimental_context; `biologicalProcessFullText`, `molecularFunctionFullText`, `cellularComponentFullText`, `ecNumberFullText`, `keggFullText`, `cogCategoryFullText`, `cyanorakRoleFullText`, `tigrRoleFullText` on ontology/role node `name` properties)
- Unified ontology `level` property: every term on `BiologicalProcess`, `MolecularFunction`, `CellularComponent`, `EcNumber`, `KeggTerm`, `CyanorakRole`, `TigrRole`, `CogFunctionalCategory`, `Pfam`, `PfamClan`, `BriteCategory` carries `level: int` with `0` = broadest/root. GO DAG terms whose min-path ≠ max-path to the namespace root additionally carry `level_is_best_effort = "true"` (sparse — property absent otherwise; check with `t.level_is_best_effort IS NOT NULL`). KEGG's original string `level` has been renamed to `level_kind` (`category|subcategory|pathway|ko`); the new integer `level` maps `category=0, subcategory=1, pathway=2, ko=3`. See `docs/kg-changes/ontology-level.md`.
- KeggTerm pathway-level nodes (level_kind = 'pathway', ~377 nodes) carry post-import `reaction_count: int` (incoming Reaction_in_kegg_pathway edges) and `metabolite_count: int` (incoming Metabolite_in_pathway edges). Sparse — only on pathway-level KeggTerm nodes; KOs and category nodes have neither.
- `adjusted_p_value` may be null on expression edges when the original study did not report it
- Strains in graph: MED4, AS9601, MIT9301, MIT9312, MIT9313, MIT9303, NATL1A, NATL2A, RSP50, **SS120** (Prochlorococcus); CC9311, WH7803, PCC7002, PCC7942, UTEX2973, **BL107** (Synechococcus); WH8102 (Parasynechococcus); BP1 (Thermosynechococcus); MIT1002, EZ55, HOT1A3, ATCC27126, AD45, BS11, BGP6, **AltDE**, **AltDE1** (Alteromonas); W3-18-1 (Shewanella); KT2440 (Pseudomonas); DSS-3 (Ruegeria); MruberA (Meiothermus). Reference proteome match organisms (not cultured strains): **Marinobacter (MarRef v6)** (HP15 reference proteome, GCF_000166295.1), **Alteromonas (MarRef v6)** (Alt_MarRef, GCA_003513035.1, DEH24_ locus tags). New as of 2026-04-15: SS120, BL107, HP15, AltMedDE (added for Domínguez 2017 + Fuszard 2012 + Moreno 2023 integration); HP15 renamed to Marinobacter (MarRef v6), AltMedDE renamed to Alt_MarRef as of 2026-04-16 — see `docs/community_proteomics_marref_saga.md` and `docs/kg-changes/reference-proteome-match-organisms.md`. New as of 2026-05-24: **AltDE** (*Alteromonas mediterranea* DE, GCF_000020585.3, taxid 1774373) + **AltDE1** (*A. mediterranea* DE1, GCF_000310085.1, taxid 1004786) — deep-ecotype genome strains, genomes only (no DE paper); distinct from the Alt_MarRef MarRef proxy. The `"alteromonas mediterranea de"` gene-ID alias now resolves to the real AltDE genome (previously the Alt_MarRef proxy). New as of 2026-06: **MIT1327** (*Prochlorococcus* LLIV, GCF_001632125.1, taxid 1801626) + **MIT1314** (*Prochlorococcus* HLII, GCF_034093315.1, taxid 3096220) — the two deferred Soussan 2025 panel strains, now with public assemblies (genomes only, no DE paper). MIT1327 carries CyanoRak annotation (CK clusters + roles) bridged from a CyanoRak-team gbff via diamond protein matching, because the strain has no public CyanoRak server export — see `multiomics_kg/download/convert_cyanorak_gbff_to_gff.py`. MIT1314 is NCBI/eggNOG-only (no CyanoRak).

## EggNOG Mapper Setup

EggNOG mapper is installed as a project dependency (`eggnog-mapper>=2.1.13` in `pyproject.toml`).
Set `EGGNOG_DATA_DIR` in `.env` to point to the database directory (e.g. `~/tools/eggnog-mapper`).

**Known bug (v2.1.13):** `download_eggnog_data.py` uses the wrong base URL (`eggnogdb.embl.de` → 404).
After `uv sync`, patch `.venv/bin/download_eggnog_data.py` lines 15 and 18:
```python
# Wrong (old domain, returns 404):
BASE_URL = f'http://eggnogdb.embl.de/download/emapperdb-{__DB_VERSION__}'
NOVEL_FAMS_BASE_URL = f'http://eggnogdb.embl.de/download/novel_fams-{__NOVEL_FAMS_DB_VERSION__}'
# Correct:
BASE_URL = f'http://eggnog5.embl.de/download/emapperdb-{__DB_VERSION__}'
NOVEL_FAMS_BASE_URL = f'http://eggnog5.embl.de/download/novel_fams-{__NOVEL_FAMS_DB_VERSION__}'
```
See: https://github.com/eggnogdb/eggnog-mapper/issues/575

## Known Issues

- **~46% of UniProt proteins are orphaned (no Protein_belongs_to_organism or Gene_encodes_protein edges)** — The UniProt adapter only creates these edges when a protein's RefSeq WP_ ID matches an entry in `gene_mapping.csv`. UniProt returns proteins for a taxid regardless of whether they have a WP_ cross-reference, so proteins absent from our NCBI gene_mapping are stranded. Investigation needed: check whether this fraction is a pre-existing data gap or a regression from the Feb 2026 UniProt adapter refactor (`fe5c2bb`). See `plans/orphan_proteins.md` for details. The KG validity tests `test_no_orphan_proteins` and `test_no_orphan_proteins_without_gene` were documented as failing because of this, but **did not reproduce on the 2026-08-17 rebuild** (`pytest -m kg` → both passed clean, as part of the InterPro multi-ontology redesign's verification — see `docs/kg-changes/interpro-multi-ontology.md`). Not investigated further as part of that work; verify on the next rebuild before closing this entry.

- **Cyanorak web server is intermittently unavailable** — `bioinformatics.psb.ugent.be` returns connection errors or throttles requests without warning. This is a server-side issue outside our control. When it happens, use `--skip-cyanorak` (files already in cache are reused). The `TestCyanorakDownloadFile` tests in `tests/test_download_genome_data.py` may also be affected if the server is down during CI.

## Notes

- Uses custom fork of pypath-omnipath: `https://github.com/wosnat/pypath`
- `chemparse` — pure-Python Hill-notation formula parser (used in metabolism_adapter for Metabolite.elements)
- Current organisms: 19 *Prochlorococcus* (incl. **MIT1327**, **MIT1314**) + 7 *Synechococcus/Parasynechococcus* + 1 *Thermosynechococcus* (BP1) + 9 *Alteromonas* (incl. **AltDE**, **AltDE1**) + 4 heterotrophs (Shewanella, Pseudomonas, Ruegeria, Meiothermus) + 2 reference proteome match organisms (**Marinobacter (MarRef v6)**, **Alteromonas (MarRef v6)**); 47 OrganismTaxon nodes total (40 genome strains + 5 treatment organisms + 2 reference proteome match); primary focus is MED4 (NCBI taxid 59919). Authoritative per-genus breakdown lives in `tests/kg_validity/test_organism.py`.
- API keys go in `.env`: `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `ELSEVIER_API_KEY`, plus others for LangChain tracing and search
- `@pytest.mark.slow` tests run the full pipeline as a subprocess and take ~1 hour
- `@pytest.mark.kg` tests require a running Neo4j instance (Docker graph); skip automatically if not available

## Development Workflow

Every development task — feature, bug fix, refactor, or new data integration — must follow all five phases below. Do not skip phases or combine them implicitly.

### 1. Scope

Before writing any code, define the scope explicitly:
- **What** is changing (files, adapters, schema, config)
- **What is NOT changing** (boundaries prevent scope creep)
- **Acceptance criteria** — concrete, testable conditions for "done"
- For non-trivial tasks, create a plan file in `plans/` and get alignment before proceeding

### 2. Implement

- Make the code changes
- Follow existing patterns (adapter pattern, ID conventions, string sanitization)
- Keep changes minimal — only what the scope requires

### 3. Test

- Write or update unit tests for every code change
- Run the relevant test suite before considering the task complete:
  ```bash
  # Unit tests (fast, always run)
  pytest -m "not slow and not kg" -v

  # KG validity tests (when graph structure changes, requires running Neo4j)
  pytest -m kg -v
  ```
- For schema or adapter changes: use `/omics-edge-snapshot` before and after to verify no edge regressions
- For new paper integrations: use `/check-gene-ids` to validate match rates

### 4. Review

- Verify string literals are consistent across schema, adapters, and tests
- Check that no old vocabulary or dead code remains
- Confirm no unintended side effects on existing data (e.g., edge count changes)
- For schema changes: verify `schema_config.yaml`, adapter code, post-import scripts, and test fixtures all agree

### 5. Document

Update documentation to reflect the changes:
- **`CLAUDE.md`** — if the change affects build commands, architecture, key facts, or known issues
- **Plan files** (`plans/`) — mark completed phases, record decisions
- **Skill files** (`.claude/skills/`) — if the change affects skill behavior or templates
- **`CHANGELOG.md`** — log every user-visible change under `## [Unreleased]`. Code changes go in `### Added` / `### Changed` / `### Fixed`; **publications, datasets and strains go in `### Data`**; anything that changes the meaning of an existing property, count or filter goes in `### Breaking`; a new capability worth one plain-prose sentence goes in `### Highlights`. See the authoring conventions at the top of the file.
- **Memory** — if the change introduces new patterns or facts that future conversations need
