# Thompson 2011

**Citation:** Thompson AW, Huang K, Saito MA, Chisholm SW (2011). Transcriptome response of high- and low-light-adapted *Prochlorococcus* strains to changing iron availability. *The ISME Journal* 5:1580–1594.
**DOI:** 10.1038/ismej.2011.49
**Organism(s):** *Prochlorococcus* MED4 and MIT9313
**Topic:** Microarray (Cyber-T) transcriptome response of two *Prochlorococcus* ecotypes to abrupt Fe starvation (-Fe) and subsequent Fe rescue (+Fe added after ~48–53 h). Time course sampled at 12/24/48h (MED4) and 16/28/53h (MIT9313), with a "R" column representing log2(after rescue / before rescue). Values are log2(-Fe/+Fe); `*` marks q<0.01 and `**` marks q<0.01 AND |log2FC|>1.

## Available data inventory

| File | Type | Content | KG status | Recommended action |
|------|------|---------|-----------|--------------------|
| `41396_2011_article_bfismej201149.pdf` | PDF | Main paper | reference | — |
| `supp table 1.csv` | CSV | MED4 DE genes: `Gene` (e.g. `PMED4_00651 (PMM0063)`), `Description`, `Cluster`, log2FC at 0/12/24/48h + `R*`, asterisks encode q-value thresholds | already in | — (see action 1 below about the `Cluster` column) |
| `supp table 2.csv` | CSV | MIT9313 DE genes: same schema as supp table 1 with timepoints 0/16/28/53h + `R*` | already in | — (see action 1) |
| `supp table legend.txt` | TXT | Legend for supp table 1 & 2 (q-value coding, cluster reference to Figure 3) | reference | — |
| `41396_2011_bfismej201149_moesm53_esm.doc` | DOC | Supplementary text/methods document | skip | — |
| `41396_2011_bfismej201149_moesm54_esm.pdf` | PDF | Supplementary figure/methods | reference | — |
| `41396_2011_bfismej201149_moesm55_esm.pdf` | PDF | Supplementary figure/methods | reference | — |
| `extract_tables_from_pdf.py` | Python | Helper script that originally extracted the supp tables from the PDF | skip | — |
| `paperconfig.yaml` | YAML | Active paperconfig | already in | — |
| `paperconfig_orig.yaml` | YAML | Pre-resolution copy of paperconfig | reference | — |
| `supp table 1_resolved.csv` / `_resolved_report.txt` | CSV / TXT | Auto-generated by prepare_data step 4 | already in | — |
| `supp table 2_resolved.csv` / `_resolved_report.txt` | CSV / TXT | Auto-generated by prepare_data step 4 | already in | — |

## Classification

**Bucket B — new metrics / DE / resolution (want to add)**

DE component (8 statistical analyses across MED4 + MIT9313 Fe starvation/rescue time courses) is fully integrated. Outstanding extension: both supp tables carry a `Cluster` column (paper Figure 3 hierarchical clustering) that has not been emitted as `gene_clusters` or `derived_metrics_table` entries yet. This is per-gene categorical evidence that maps cleanly to a Gene-attached metric and can be added by extending the existing paperconfig — no new strain or schema change needed. Both strains (MED4, MIT9313) are deployed.

## Current paperconfig summary

- Experiments defined: 6 — MED4 -Fe (`iron_iron_starvation_med4_med4_microarray_light`, plus a `_2` variant noting 48h +Fe sample drop-out), MED4 rescue, MIT9313 -Fe, MIT9313 rescue (4 scientific comparisons; 5 Experiment nodes + 1 control-oriented duplicate)
- Statistical analyses (DE edges): 8 — 4 timepoints per strain × 2 (MED4: 12/24/48h + R; MIT9313: 16/28/53h + R)
- Supplementary materials entry types: 2× `csv`
- Organisms covered: *Prochlorococcus* MED4, *Prochlorococcus* MIT9313
- Table scope(s): `significant_any_timepoint` (the tables contain only genes that were DE at some point)
- Non-DE evidence: none currently emitted. Both supp tables carry a `Cluster` column (hierarchical cluster IDs referenced by paper Figure 3) that is not integrated.
- ID resolution: `Gene` column uses `old_locus_tag` style (`PMED4_#####` or `P9313_#####`, often with a parenthetical secondary synonym like `PMM0063` / `PMT0107`). Resolved via `old_locus_tag` tier-1 lookup; see `_resolved_report.txt` for match rates.

## Recommended actions

1. **Add** — Extract the `Cluster` column from `supp table 1.csv` and `supp table 2.csv` as `gene_clusters` entries (one per strain; `cluster_type: hierarchical`, `cluster_method: hierarchical clustering (see paper Fig 3)`). Attach to the matching MED4 and MIT9313 iron-starvation experiments. Alternatively integrate as a `derived_metrics_table` with a single categorical column if cluster-level descriptions are not available.
2. **Skip** — `extract_tables_from_pdf.py` is a preprocessing helper; not data.
3. **No action** — `.doc` and supplementary PDFs are reference materials (methods text, figures); keep for provenance.

## Notes

- Gene ID format: `{PMED4_#####|P9313_#####} (synonym)`; the whitespace and parenthetical synonym are parsed by the v2 mapping pipeline. Both `PMED4_` and `P9313_` are `old_locus_tag` style and are bridged to NCBI RS-style locus tags.
- Significance encoding: `pvalue_asterisk_in_logfc: true` — asterisks in the log2FC cell encode significance rather than a separate q-value column. `pvalue_threshold: 0.01`, `logfc_threshold: 1.0`.
- The `iron_iron_starvation_med4_med4_microarray_light_2` experiment exists specifically because the 48h +Fe sample hybridized poorly; 48h -Fe was compared to 24h +Fe instead. Treat as a distinct Experiment node.
- "R" (rescue) timepoints have `timepoint_hours: null` — they encode fold change after Fe addition, not a clock time.
